---
"@opengeni/core": minor
"@opengeni/react": minor
"@opengeni/api-router": patch
---

Add a public atomic composer application command shared by HTTP and in-process hosts, a receiver-safe narrow React session client constructor with host submit overrides and draft projection, and presentation-only file-node filtering across the packaged workbench surfaces.