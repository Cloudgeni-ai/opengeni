---
"@opengeni/react": patch
---

Give each session-only React hook an exact structural client contract so embedded hosts can use `useSession`, `useGoal`, `useSessionLineage`, and `useFileAttachments` without implementing unrelated workspace or workbench APIs.
