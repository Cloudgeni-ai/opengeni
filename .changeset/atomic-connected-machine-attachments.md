---
"@opengeni/contracts": patch
"@opengeni/db": patch
"@opengeni/sdk": patch
---

Attach a selected user-owned Connected Machine atomically when creating a session from the managed web console, including recovery-stable `once` authority.
