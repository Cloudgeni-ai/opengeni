---
"@opengeni/react": patch
---

Hide the agent workspace's machine-state chip while viewing independent Browser or Desktop resources. Those viewers retain their own runtime status, so a sleeping agent sandbox no longer makes an active browser appear asleep.
