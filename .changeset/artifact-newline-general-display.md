---
"@opengeni/react": patch
---

Keep document typing and paste after a trailing newline, ignore IME composition Enter, and show spreadsheet General numbers without binary residue while leaving stored values and formula-bar source unchanged.
