---
"@opengeni/react": patch
---

Clear file attachments when their client or workspace changes, revoke every outstanding preview URL on unmount even before its React state commits, fence late upload settlements from stale scopes, and reset Changes-tab virtualization measurements when a same-size diff is replaced.
