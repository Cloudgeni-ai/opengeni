---
"@opengeni/contracts": patch
"@opengeni/runtime": patch
"@opengeni/worker-bundle": patch
---

Advertise cwd-relative sandbox file paths to the model, and return the SDK execCommand banner (exit code + stdout/stderr) from Connected Machines.
