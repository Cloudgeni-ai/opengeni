---
"@opengeni/contracts": patch
"@opengeni/runtime": patch
"@opengeni/db": patch
---

Persist configured skill catalogs as versioned conversation context, preserving prior prompt history across catalog updates, retries, compaction, and forks.
