---
"@opengeni/config": patch
"@opengeni/deployment": patch
"@opengeni/storage": patch
---

Keep browser-facing S3-compatible signed URLs on the public endpoint while routing authenticated API and worker object operations through an optional internal endpoint.
