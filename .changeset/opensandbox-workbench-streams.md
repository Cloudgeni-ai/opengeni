---
"@opengeni/api-router": patch
"@opengeni/contracts": patch
"@opengeni/react": patch
"@opengeni/runtime": patch
---

Route OpenSandbox browser and computer streams through the API frame-proxy so the workbench can show live JPEG/RFB when the lifecycle proxy cannot carry browserd WebSocket grants.
