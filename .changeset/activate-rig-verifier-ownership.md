---
"@opengeni/worker-bundle": patch
---

Add the second-phase, default-off rig-verifier ownership activation path. Reject before provider creation while disabled, durably attribute every exact created instance before setup, and independently deactivate ambiguous registrations and terminate the provider on every exit.
