---
"@opengeni/api-router": patch
"@opengeni/react": patch
---

Show human GitHub connection setup in chat when an agent encounters an unbound workspace. Continue account selection and installation through owner authorization without an extra launch click, and open repository configuration in a separate tab.
