---
"@opengeni/runtime": patch
"@opengeni/api-router": patch
---

Surface Gmail startup authorization failures in the conversation and distinguish enabled personal integrations from tools available to the current attempt. Personal access continues to require the owner's explicit session grant.
