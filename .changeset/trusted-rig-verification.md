---
"@opengeni/contracts": patch
"@opengeni/config": patch
"@opengeni/core": patch
"@opengeni/db": patch
"@opengeni/react": patch
"@opengeni/runtime": patch
"@opengeni/sdk": patch
---

Harden Rig verification provenance with provider metadata-backed platform runtime manifests that reject symlinks and non-regular helpers, attest ELF interpreters and recursive shared-library/loader-input closure, use fresh pristine authority sidecars for each protected phase, revalidate candidates around Terminal/Browser/Computer execution, require Docker and Modal immutable-image cold-boot proof, durably recover and reclaim exact Docker provider images, enforce deadlines, preserve actor attribution, and recover exact client versions.