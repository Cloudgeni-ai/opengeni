---
"@opengeni/codex": patch
"@opengeni/config": patch
"@opengeni/runtime": patch
---

Add opt-in durable GPT-6 Astra reasoning effort updates with a stable request-level baseline, retry fencing, SDK replay, and explicit compaction restoration. Disabled by default pending live backend verification.
