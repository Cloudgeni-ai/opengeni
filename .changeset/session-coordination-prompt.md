---
"@opengeni/runtime": patch
---

Put session-coordination doctrine in the non-bypassable operational prompt: use session tools for user-requested session management, and spawn a child worker for a subtask instead of hijacking an unrelated existing session.
