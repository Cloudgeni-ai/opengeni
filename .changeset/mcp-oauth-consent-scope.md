---
"@opengeni/api-router": patch
"@opengeni/db": patch
---

Polish MCP OAuth consent for every client: shared OpenGeni chrome, organization and workspace pickers at authorize time, tokens that follow the chosen workspace, and token exchange that accepts any registered redirect URI.
