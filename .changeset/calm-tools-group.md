---
"@opengeni/contracts": minor
"@opengeni/core": minor
"@opengeni/sdk": minor
---

Workspaces can persist exact default MCP servers and first-party OpenGeni tools for new sessions. Session creation, workspace-default policy updates, and scheduled session creation apply that policy while preserving the previous deployment defaults for workspaces without an override.
