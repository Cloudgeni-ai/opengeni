---
"@opengeni/worker-bundle": patch
---

Project supported ready file attachments with verified size and canonical SHA-256 metadata into typed image and file content for Responses model turns while preserving the sandbox-path fallback and durable history invariants.
