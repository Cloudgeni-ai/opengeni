---
"@opengeni/worker-bundle": patch
---

Project supported ready file attachments into typed image and file content for Responses model turns while preserving the sandbox-path fallback and durable history invariants.