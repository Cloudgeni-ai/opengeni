---
"@opengeni/runtime": patch
"@opengeni/worker-bundle": patch
---

Normalize JavaScript-only undefined object fields from SDK history before durable JSON persistence while rejecting other non-JSON values with an exact path.
