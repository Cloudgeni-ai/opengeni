---
"@opengeni/api-router": patch
---

Repair Slack bot workspace-choice and single-select question buttons while preserving existing message retry receipts. Link new routed-task workspace labels and retain a dismissible identity-link completion notice through its access refresh. Verify personal Slack session consent, accepted-turn access, and revocation independently of private-session activation.

Show selected personal integrations that still need exact-chat consent above the composer, with a direct route to the existing consent card. Reconnecting an account no longer leaves the missing chat authorization undiscoverable.
