---
"@opengeni/react": patch
---

Show unavailable saved changes previews inside the affected Changes view instead of displaying capture-error notifications when a session opens. Preserve the existing explicit live-workspace action and the live-only Staged view.