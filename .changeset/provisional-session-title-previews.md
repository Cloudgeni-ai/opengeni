---
"@opengeni/sdk": minor
"@opengeni/react": patch
"@opengeni/contracts": minor
---

Show a bounded, sensitive-safe opening-prompt preview while a session's durable automatic title is still pending, then yield automatically to the semantic agent title or a human rename.
