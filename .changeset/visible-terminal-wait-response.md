---
"@opengeni/react": patch
---

Keep a durable wait reason visible after a tool-only turn settles without assistant prose, including legacy goal holds, while preserving collapsed step detail and avoiding duplicate status text when a real response exists.