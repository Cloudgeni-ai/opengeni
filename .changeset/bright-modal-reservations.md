---
"@opengeni/config": patch
"@opengeni/runtime": patch
"@opengeni/worker-bundle": patch
---

Add optional Modal CPU and memory reservations and preserve them through sandbox creation, exact resume, and snapshot replacement.