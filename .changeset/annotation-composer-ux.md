---
"@opengeni/react": patch
---

Group composer timeline annotations into one count chip, pin numbered badges on the quoted source text, and keep Add note available for long highlights that end in message chrome. Dense and long notes stay in a viewport-clamped review list, stacked badges separate, and sent cards collapse instead of growing without bound.
