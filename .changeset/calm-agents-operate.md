---
"@opengeni/runtime": patch
"@opengeni/contracts": patch
"@opengeni/db": patch
"@opengeni/worker-bundle": patch
---

Add a provider-neutral operational instruction contract for consistent agent collaboration, execution safety, file editing, and skill usage across every OpenGeni persona. Keep persistent system instructions free of mutable display metadata and append each accepted turn's frozen goal to its newest durable model-input item.
