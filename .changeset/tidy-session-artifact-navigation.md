---
"@opengeni/react": patch
---

Allow hosts to open a workbench tab with `openTabRequest`. The stock console routes Site links into the session artifact panel and retains a session return link when expanding Sites or editors full-page.