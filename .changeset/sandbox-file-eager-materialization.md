---
"@opengeni/worker-bundle": patch
---

Stop inlining documents as Codex `input_file` (ZDR 400). Keep attaching-turn sandbox copies. Connected Machines sign MinIO with the public endpoint, not Docker DNS.
