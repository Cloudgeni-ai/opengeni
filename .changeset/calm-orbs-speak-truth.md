---
"@opengeni/api-router": patch
"@opengeni/codex": patch
"@opengeni/config": patch
"@opengeni/contracts": patch
"@opengeni/core": patch
"@opengeni/db": patch
"@opengeni/react": patch
"@opengeni/sdk": patch
---

Add an experimental session-bound full-duplex realtime voice boundary, short-lived OpenGeni grant
contracts, serialized idempotent transcript delivery, explicit retention and policy-limit hooks,
SDK and React lifecycle surfaces, a persistent selected/workspace-main orb, and a validated general
workspace-main session setting while keeping production fail-closed until the Codex-subscription
audio protocol and server gateway are independently proven.