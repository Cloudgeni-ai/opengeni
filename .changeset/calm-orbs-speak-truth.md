---
"@opengeni/api-router": patch
"@opengeni/codex": patch
"@opengeni/config": patch
"@opengeni/contracts": patch
"@opengeni/react": patch
"@opengeni/sdk": patch
---

Add an experimental session-bound full-duplex realtime voice boundary, short-lived OpenGeni grant
contracts, SDK and React lifecycle surfaces, and a persistent session orb while keeping production
fail-closed until the Codex-subscription audio protocol and server gateway are independently proven.