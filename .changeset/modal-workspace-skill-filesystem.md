---
"@opengeni/runtime": patch
---

Allow Modal-backed repository sessions to enumerate workspace skill directories so optional discovery does not fail before a turn and repository skills remain available after resume. Keep the shared filesystem confinement path functional on stock macOS as well as GNU/Linux.
