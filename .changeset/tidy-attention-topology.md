---
"@opengeni/api-router": patch
---

Search every descendant depth when agent topology is scoped to an explicit root session. Explicit parent filters still narrow the query to that parent, and cursor validation preserves the original scope.
