---
"@opengeni/react": patch
---

Add a compact accessible mobile slide selector so presentation editors can jump to any slide when the desktop rail is hidden below `sm`, without changing desktop rail virtualization or keyboard selection.
