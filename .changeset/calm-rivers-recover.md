---
"@opengeni/runtime": patch
"@opengeni/worker-bundle": patch
---

Recover required MCP setup when a transient socket failure is wrapped by the MCP SDK, while preserving only secret-safe transport classification.
