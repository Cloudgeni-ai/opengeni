---
"@opengeni/core": patch
---

Use an existing target session's persisted MCP configuration when admitting scheduled host-tool authority. Preserve exact destination matching and accepted-attempt delegation checks without inheriting authority from ordinary inline credentials.