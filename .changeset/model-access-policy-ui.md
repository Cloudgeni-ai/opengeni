---
"@opengeni/sdk": patch
"@opengeni/contracts": patch
---

Add typed workspace model-access policy reads and full-replacement updates for admin settings surfaces, plus a provider-private per-model policy verdict in the authenticated catalog.