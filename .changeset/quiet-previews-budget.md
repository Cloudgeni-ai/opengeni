---
"@opengeni/api-router": patch
---

Bound opt-in `sessions_list` latest-message previews by a deterministic aggregate UTF-8 budget.
Rows that exceed the budget remain discoverable with explicit omission metadata and a
`session_get` drill-down route, while the existing response envelope and pagination cap remain
independent.