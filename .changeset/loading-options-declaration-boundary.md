---
"@opengeni/react": patch
---

Keep loading animation options self-contained in public declarations so NodeNext consumers do not need to resolve the renderer's internal type imports. Supported animation options and runtime behavior are unchanged.