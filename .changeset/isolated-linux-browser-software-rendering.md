---
"@opengeni/browserd": patch
---

Use software Chromium rendering on controller-allocated isolated Linux displays. Allow an explicit Linux operator opt-in for externally provisioned virtual displays; other browser launch paths keep their defaults.
