---
"@opengeni/runtime": patch
"@opengeni/db": patch
"@opengeni/react": patch
"@opengeni/worker-bundle": patch
---

Use provider-reported usage rather than whole-request approximations for automatic context compaction, preserve provider-only input-token state across context rewrites, and label timeline counts as estimated conversation-history tokens.
