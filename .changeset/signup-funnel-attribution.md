---
"@opengeni/contracts": minor
---

Add first-touch sign-up attribution contracts: the closed-charset `SignupAttribution` schema with its URL parameter names, the `SIGNUP_ACQUISITION_SOURCES` channel set, and `signupAcquisitionSource`, which normalizes untrusted campaign parameters into `producthunt`, `website`, `direct`, or `other` for bounded sign-up metrics.
