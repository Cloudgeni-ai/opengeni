---
"@opengeni/sdk": patch
---

Align queued and steered message inputs with the API contract, expose the accepted control and connection-authority fields, and reject the retired per-message tools field before transport.