---
"@opengeni/storage": patch
"@opengeni/worker-bundle": patch
"@opengeni/api-router": patch
"@opengeni/documents": patch
---

Treat a resolved object PUT as the write. Expected-present reads retry not-found. Screenshot history re-resolves from the artifact row instead of a sticky unavailable receipt.
