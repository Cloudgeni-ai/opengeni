---
"@opengeni/sdk": patch
---

Accept web-standard custom fetch responses without requiring Bun's non-standard `Response.textStream()` extension or the newer, non-universal `Response.bytes()` method.
