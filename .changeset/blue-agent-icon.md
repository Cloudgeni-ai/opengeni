---
"@opengeni/api-router": patch
"@opengeni/config": patch
---

Ship the branded macOS Connected Machine icon from every control plane and
promote the existing signed agent 0.1.15 release as the default stable channel.
