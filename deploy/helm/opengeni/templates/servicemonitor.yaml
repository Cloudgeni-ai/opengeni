{{- if and .Values.api.enabled ((.Values.observability).serviceMonitor).enabled (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "opengeni.fullname" . }}-api
  labels:
    {{- include "opengeni.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
    {{- with ((.Values.observability).serviceMonitor).labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with ((.Values.observability).serviceMonitor).annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "opengeni.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
      {{- if .Values.api.metricsPort }}
      opengeni.ai/metrics-endpoint: "true"
      {{- end }}
  endpoints:
    {{- /* The internal listener when api.metricsPort is set; http only in the legacy configuration. */}}
    - port: {{ if .Values.api.metricsPort }}metrics{{ else }}http{{ end }}
      path: {{ ((.Values.observability).metrics).path | default "/metrics" | quote }}
      interval: {{ ((.Values.observability).serviceMonitor).interval | default "30s" | quote }}
      scrapeTimeout: {{ ((.Values.observability).serviceMonitor).scrapeTimeout | default "10s" | quote }}
      relabelings:
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
        - sourceLabels: [__meta_kubernetes_service_label_app_kubernetes_io_instance]
          targetLabel: release
        - sourceLabels: [__meta_kubernetes_service_label_app_kubernetes_io_component]
          targetLabel: opengeni_workload_component
        {{- if .Values.api.metricsPort }}
        # Keep the API series identity of the public API Service, so alerts and
        # dashboards keyed on service/job continue to match the moved target.
        - targetLabel: service
          replacement: {{ include "opengeni.fullname" . }}-api
        - targetLabel: job
          replacement: {{ include "opengeni.fullname" . }}-api
        {{- end }}
{{- end }}
{{- if and .Values.worker.enabled ((.Values.observability).serviceMonitor).enabled (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") }}
{{- range $role := list "control" "turns" }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "opengeni.fullname" $ }}-worker-{{ $role }}
  labels:
    {{- include "opengeni.labels" $ | nindent 4 }}
    app.kubernetes.io/component: worker-{{ $role }}
    {{- with (($.Values.observability).serviceMonitor).labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with (($.Values.observability).serviceMonitor).annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "opengeni.selectorLabels" $ | nindent 6 }}
      app.kubernetes.io/component: worker-{{ $role }}
  endpoints:
    - port: http
      path: {{ (($.Values.observability).metrics).path | default "/metrics" | quote }}
      interval: {{ (($.Values.observability).serviceMonitor).interval | default "30s" | quote }}
      scrapeTimeout: {{ (($.Values.observability).serviceMonitor).scrapeTimeout | default "10s" | quote }}
      relabelings:
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
        - sourceLabels: [__meta_kubernetes_service_label_app_kubernetes_io_instance]
          targetLabel: release
        - sourceLabels: [__meta_kubernetes_service_label_app_kubernetes_io_component]
          targetLabel: opengeni_workload_component
{{- end }}
{{- end }}
{{- if and .Values.relay.enabled ((.Values.observability).serviceMonitor).enabled (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "opengeni.fullname" . }}-relay
  labels:
    {{- include "opengeni.labels" . | nindent 4 }}
    app.kubernetes.io/component: relay
    {{- with ((.Values.observability).serviceMonitor).labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with ((.Values.observability).serviceMonitor).annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "opengeni.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: relay
  endpoints:
    # The relay exposes a Prometheus exposition at GET /metrics on the wss port.
    - port: wss
      path: /metrics
      interval: {{ ((.Values.observability).serviceMonitor).interval | default "30s" | quote }}
      scrapeTimeout: {{ ((.Values.observability).serviceMonitor).scrapeTimeout | default "10s" | quote }}
      relabelings:
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
        - sourceLabels: [__meta_kubernetes_service_label_app_kubernetes_io_instance]
          targetLabel: release
{{- end }}
