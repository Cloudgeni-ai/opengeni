# Safe fallback for direct `docker compose up` outside scripts/dev-stack.sh.
# The full dev stack renders an ignored auth-callout configuration and points
# OPENGENI_NATS_CONFIG_FILE at it before starting Compose.
port: 4222
http: 8222
