diff --git a/node_modules/.bun/@openai+agents-core@0.14.3+933c5e7db4cd33c2/node_modules/@openai/agents-core/.bun-tag-1a92431e4f62cd51 b/.bun-tag-1a92431e4f62cd51
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/.bun/@openai+agents-core@0.14.3+933c5e7db4cd33c2/node_modules/@openai/agents-core/.bun-tag-2da6ec976600e880 b/.bun-tag-2da6ec976600e880
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/.bun/@openai+agents-core@0.14.3+933c5e7db4cd33c2/node_modules/@openai/agents-core/.bun-tag-7c21d377cb19cf3e b/.bun-tag-7c21d377cb19cf3e
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/.bun/@openai+agents-core@0.14.3+933c5e7db4cd33c2/node_modules/@openai/agents-core/.bun-tag-ac46de758592501 b/.bun-tag-ac46de758592501
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 21b58188472152339a1747df5cfe8dd256356cd1..1ecb7bff25d73312059991d05d44978f9331b033 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -26,10 +26,10 @@ export { setDefaultModelProvider } from './providers';
 export { retryPolicies } from './runner/modelRetry';
 export { RunResult, StreamedRunResult } from './result';
 export { IndividualRunOptions, NonStreamRunOptions, run, RunConfig, Runner, StreamRunOptions, } from './run';
-export type { ModelInputData, CallModelInputFilter, CallModelInputFilterArgs, ToolErrorFormatter, ToolErrorFormatterArgs, ToolExecutionConfig, ToolNameCollisionPolicy, ToolNotFoundBehavior, ToolErrorKind, ReasoningItemIdPolicy, RunErrorData, RunErrorHandler, RunErrorHandlerInput, RunErrorHandlerResult, RunErrorHandlers, RunErrorKind, } from './run';
+export type { ModelInputData, CallModelInputFilter, CallModelInputFilterArgs, ToolErrorFormatter, ToolErrorFormatterArgs, ToolExecutionConfig, ToolNameCollisionPolicy, ToolNotFoundBehavior, ResolveMissingFunctionTool, ToolErrorKind, HistoryOwnership, ModelResponseRetention, ReasoningItemIdPolicy, RunErrorData, RunErrorHandler, RunErrorHandlerInput, RunErrorHandlerResult, RunErrorHandlers, RunErrorKind, } from './run';
 export { RunContext } from './runContext';
 export type { AgentToolInvocation } from './agentToolInvocation';
-export { RunState } from './runState';
+export { RunState, type ClientToolSearchRehydration, type RunStateDeserializationOptions, } from './runState';
 export type { TracingConfig } from './tracing';
 export { HostedTool, attachClientToolSearchExecutor, ComputerTool, computerTool, ShellTool, shellTool, ApplyPatchTool, applyPatchTool, HostedMCPTool, hostedMcpTool, FunctionTool, FunctionToolResult, FunctionToolTimeoutBehavior, ToolTimeoutErrorFunction, Tool, tool, toolNamespace, invokeFunctionTool, getClientToolSearchExecutor, getToolSearchRuntimeToolKey, ToolExecuteArgument, ToolEnabledFunction, ToolOptionsWithGuardrails, ToolAllowedCaller, ToolAllowedCallers, } from './tool';
 export type { ClientToolSearchExecutor, ClientToolSearchExecutorArgs, ClientToolSearchExecutorResult, ComputerOnSafetyCheckFunction, ComputerSafetyCheck, ComputerSafetyCheckResult, ShellToolEnvironment, ShellToolLocalEnvironment, ShellToolLocalSkill, ShellToolHostedEnvironment, ShellToolContainerAutoEnvironment, ShellToolContainerReferenceEnvironment, ShellToolContainerSkill, ShellToolSkillReference, ShellToolInlineSkill, ShellToolInlineSkillSource, ShellToolContainerNetworkPolicy, ShellToolContainerNetworkPolicyAllowlist, ShellToolContainerNetworkPolicyDisabled, ShellToolContainerNetworkPolicyDomainSecret, ToolInputParameters, ToolOutputSchema, ToolOptions, ToolNamespaceOptions, ToolOutputCustomData, FunctionToolCustomDataContext, FunctionToolCustomDataExtractor, ComputerToolCustomDataContext, ComputerToolCustomDataExtractor, ApplyPatchToolCustomDataContext, ApplyPatchToolCustomDataExtractor, } from './tool';
diff --git a/dist/run.d.ts b/dist/run.d.ts
index 7863d754f4d6a5e1bfb99b8cde39417796533741..abe4b6f6c6e9ac8824a0b5d0ae1b909715db5c1a 100644
--- a/dist/run.d.ts
+++ b/dist/run.d.ts
@@ -53,6 +53,27 @@ export type ToolErrorFormatter<TContext = unknown> = (args: ToolErrorFormatterAr
  * SDK-side execution settings for local tool calls.
  */
 export type ToolNotFoundBehavior = 'raise_error' | 'return_error_to_model';
+/**
+ * Resolves a function tool that is not on the current configured Agent list.
+ * Used for progressive disclosure: deferred tools stay off the first-request
+ * schema but remain executable when the model names them.
+ */
+export type ResolveMissingFunctionTool = (args: {
+    name: string;
+    agent: Agent<any, any>;
+    toolCall: unknown;
+}) => Promise<import('./tool').FunctionTool | null | undefined> | import('./tool').FunctionTool | null | undefined;
+/**
+ * Selects the owner of conversation history supplied to a run.
+ *
+ * - `sdk` preserves the default Session and server-continuation behavior.
+ * - `external` borrows explicit application-owned history as immutable input.
+ *   It avoids deep history/session snapshots while preserving RunState
+ *   interruption/resume state. Callers and input filters must not mutate it.
+ */
+export type HistoryOwnership = 'sdk' | 'external';
+/** Controls how many raw ModelResponse objects a run retains. */
+export type ModelResponseRetention = 'all' | 'last';
 /**
  * Configures settings for the entire agent run.
  */
@@ -152,6 +173,18 @@ export type RunConfig = {
      * system instructions or input items that will be sent to the model.
      */
     callModelInputFilter?: CallModelInputFilter;
+    /**
+     * Selects SDK-managed or explicit application-owned conversation history.
+     * External ownership cannot be combined with Session, session callbacks,
+     * previousResponseId, or conversationId. The supplied history is borrowed
+     * immutable input; callers and filters must return projections, not mutate it.
+     */
+    historyOwnership?: HistoryOwnership;
+    /**
+     * Retain every raw model response (default) or only the latest one.
+     * Generated items, aggregate usage, model behavior, and recovery remain complete.
+     */
+    modelResponseRetention?: ModelResponseRetention;
     /**
      * Formats tool error messages that are returned to the model.
      * Returning `undefined` falls back to the SDK default message.
@@ -161,6 +194,11 @@ export type RunConfig = {
      * Controls how run items are converted into model input for subsequent turns.
      */
     reasoningItemIdPolicy?: ReasoningItemIdPolicy;
+    /**
+     * Optional resolver for function calls whose name is missing from the
+     * current configured tool list. Unset preserves raise/return-error behavior.
+     */
+    resolveMissingFunctionTool?: ResolveMissingFunctionTool;
 };
 /**
  * Common run options shared between streaming and non-streaming execution pathways.
@@ -174,12 +212,15 @@ type SharedRunOptions<TContext = undefined, TAgent extends Agent<any, any> = Age
     session?: Session;
     sessionInputCallback?: SessionInputCallback;
     callModelInputFilter?: CallModelInputFilter;
+    historyOwnership?: HistoryOwnership;
+    modelResponseRetention?: ModelResponseRetention;
     toolErrorFormatter?: ToolErrorFormatter;
     reasoningItemIdPolicy?: ReasoningItemIdPolicy;
     tracing?: TracingConfig;
     sandbox?: SandboxRunConfig;
     toolExecution?: ToolExecutionConfig;
     toolNotFoundBehavior?: ToolNotFoundBehavior;
+    resolveMissingFunctionTool?: ResolveMissingFunctionTool;
     toolNameCollisionPolicy?: ToolNameCollisionPolicy;
     /**
      * Error handlers keyed by error kind.
diff --git a/dist/run.js b/dist/run.js
index 030a757c3b8a6037ec1e5a1b9dd81233983512a1..26c81179a25bda6743ea371438d1ec8b3c49303d 100644
--- a/dist/run.js
+++ b/dist/run.js
@@ -99,6 +99,20 @@ class LazyDefaultModelProvider {
 function isNoopTrace(trace) {
     return trace instanceof traces_1.NoopTrace || trace?.traceId === utils_1.NOOP_TRACE_OR_SPAN_ID;
 }
+function resolveHistoryOwnership(value) {
+    const resolved = value ?? 'sdk';
+    if (resolved !== 'sdk' && resolved !== 'external') {
+        throw new errors_1.UserError("historyOwnership must be either 'sdk' or 'external'.");
+    }
+    return resolved;
+}
+function resolveModelResponseRetention(value) {
+    const resolved = value ?? 'all';
+    if (resolved !== 'all' && resolved !== 'last') {
+        throw new errors_1.UserError("modelResponseRetention must be either 'all' or 'last'.");
+    }
+    return resolved;
+}
 async function run(agent, input, options) {
     const runner = getDefaultRunner();
     if (options?.stream) {
@@ -139,9 +153,12 @@ class Runner extends lifecycle_1.RunHooks {
             sandbox: config.sandbox,
             toolExecution: (0, runConfig_1.validateToolExecutionConfig)(config.toolExecution),
             toolNotFoundBehavior: config.toolNotFoundBehavior ?? 'raise_error',
+            resolveMissingFunctionTool: config.resolveMissingFunctionTool,
             toolNameCollisionPolicy: (0, runConfig_1.validateToolNameCollisionPolicy)(config.toolNameCollisionPolicy),
             sessionInputCallback: config.sessionInputCallback,
             callModelInputFilter: config.callModelInputFilter,
+            historyOwnership: resolveHistoryOwnership(config.historyOwnership),
+            modelResponseRetention: resolveModelResponseRetention(config.modelResponseRetention),
             toolErrorFormatter: config.toolErrorFormatter,
             reasoningItemIdPolicy: config.reasoningItemIdPolicy,
         };
@@ -188,6 +205,8 @@ class Runner extends lifecycle_1.RunHooks {
         const sessionInputCallback = resolvedOptions.sessionInputCallback ?? this.config.sessionInputCallback;
         // Likewise allow callers to override callModelInputFilter on individual runs.
         const callModelInputFilter = resolvedOptions.callModelInputFilter ?? this.config.callModelInputFilter;
+        const historyOwnership = resolveHistoryOwnership(resolvedOptions.historyOwnership ?? this.config.historyOwnership);
+        const modelResponseRetention = resolveModelResponseRetention(resolvedOptions.modelResponseRetention ?? this.config.modelResponseRetention);
         // Per-run callback can override runner-level tool error formatting defaults.
         const toolErrorFormatter = resolvedOptions.toolErrorFormatter ?? this.config.toolErrorFormatter;
         const reasoningItemIdPolicy = resolvedOptions.reasoningItemIdPolicy ??
@@ -195,6 +214,7 @@ class Runner extends lifecycle_1.RunHooks {
             this.config.reasoningItemIdPolicy;
         const toolExecution = (0, runConfig_1.validateToolExecutionConfig)(resolvedOptions.toolExecution ?? this.config.toolExecution);
         const toolNotFoundBehavior = resolvedOptions.toolNotFoundBehavior ?? this.config.toolNotFoundBehavior;
+        const resolveMissingFunctionTool = resolvedOptions.resolveMissingFunctionTool ?? this.config.resolveMissingFunctionTool;
         const toolNameCollisionPolicy = (0, runConfig_1.validateToolNameCollisionPolicy)(resolvedOptions.toolNameCollisionPolicy === undefined
             ? this.config.toolNameCollisionPolicy
             : resolvedOptions.toolNameCollisionPolicy);
@@ -210,10 +230,13 @@ class Runner extends lifecycle_1.RunHooks {
             ...resolvedOptions,
             sessionInputCallback,
             callModelInputFilter,
+            historyOwnership,
+            modelResponseRetention,
             toolErrorFormatter,
             reasoningItemIdPolicy,
             toolExecution,
             toolNotFoundBehavior,
+            resolveMissingFunctionTool,
             toolNameCollisionPolicy,
             tracing: tracingConfig,
         };
@@ -235,24 +258,39 @@ class Runner extends lifecycle_1.RunHooks {
         // When the server tracks conversation history we defer to it for previous turns so local session
         // persistence can focus solely on the new delta being generated in this process.
         const session = effectiveOptions.session;
+        if (historyOwnership === 'external' &&
+            (session !== undefined ||
+                sessionInputCallback !== undefined ||
+                serverManagesConversation)) {
+            throw new errors_1.UserError('External history ownership cannot be combined with Session, sessionInputCallback, previousResponseId, or conversationId.');
+        }
         let provisionalSessionHistoryTransactionSessionId;
         let provisionalSessionHistoryTransactionInputItems;
         if (resumingFromState) {
             const resumedState = input;
-            const hadSessionBinding = resumedState._currentTurnSessionHistoryTransactionSessionId !==
-                undefined;
-            const portableInputItems = resumedState._currentTurnSessionHistoryTransactionInputItems;
+            if (historyOwnership === 'external' &&
+                (resumedState._currentTurnSessionHistoryTransactionSessionId !== undefined ||
+                    resumedState._pendingSessionHistoryTransaction !== undefined ||
+                    resumedState._pendingLegacyCompactionSessionItems !== undefined)) {
+                throw new errors_1.UserError('External history ownership cannot resume RunState that is bound to SDK Session persistence.');
+            }
             resumedState.setReasoningItemIdPolicy(reasoningItemIdPolicy);
-            await (0, sessionPersistence_1.prepareSessionHistoryTransactionsForRun)(session, resumedState, {
-                serverManagesConversation,
-            });
-            if (!hadSessionBinding) {
-                provisionalSessionHistoryTransactionSessionId =
-                    resumedState._currentTurnSessionHistoryTransactionSessionId;
-                provisionalSessionHistoryTransactionInputItems = portableInputItems;
+            resumedState.setModelResponseRetention(modelResponseRetention);
+            if (historyOwnership !== 'external') {
+                const hadSessionBinding = resumedState._currentTurnSessionHistoryTransactionSessionId !==
+                    undefined;
+                const portableInputItems = resumedState._currentTurnSessionHistoryTransactionInputItems;
+                await (0, sessionPersistence_1.prepareSessionHistoryTransactionsForRun)(session, resumedState, {
+                    serverManagesConversation,
+                });
+                if (!hadSessionBinding) {
+                    provisionalSessionHistoryTransactionSessionId =
+                        resumedState._currentTurnSessionHistoryTransactionSessionId;
+                    provisionalSessionHistoryTransactionInputItems = portableInputItems;
+                }
             }
         }
-        const sessionPersistence = (0, sessionPersistence_1.createSessionPersistenceTracker)({
+        const sessionPersistence = historyOwnership === 'external' ? undefined : (0, sessionPersistence_1.createSessionPersistenceTracker)({
             session,
             hasCallModelInputFilter,
             persistInput: sessionPersistence_1.saveStreamInputToSession,
@@ -414,11 +452,13 @@ class Runner extends lifecycle_1.RunHooks {
         const hasSandboxOverride = typeof options.sandbox !== 'undefined';
         const hasToolExecutionOverride = typeof options.toolExecution !== 'undefined';
         const hasToolNotFoundBehaviorOverride = typeof options.toolNotFoundBehavior !== 'undefined';
+        const hasResolveMissingFunctionToolOverride = typeof options.resolveMissingFunctionTool !== 'undefined';
         const hasToolNameCollisionPolicyOverride = typeof options.toolNameCollisionPolicy !== 'undefined';
         const hasTracingOverride = typeof options.tracing !== 'undefined';
         if (!hasSandboxOverride &&
             !hasToolExecutionOverride &&
             !hasToolNotFoundBehaviorOverride &&
+            !hasResolveMissingFunctionToolOverride &&
             !hasToolNameCollisionPolicyOverride &&
             !hasTracingOverride) {
             return this.config;
@@ -432,6 +472,9 @@ class Runner extends lifecycle_1.RunHooks {
             ...(hasToolNotFoundBehaviorOverride
                 ? { toolNotFoundBehavior: options.toolNotFoundBehavior }
                 : {}),
+            ...(hasResolveMissingFunctionToolOverride
+                ? { resolveMissingFunctionTool: options.resolveMissingFunctionTool }
+                : {}),
             ...(hasToolNameCollisionPolicyOverride
                 ? { toolNameCollisionPolicy: options.toolNameCollisionPolicy }
                 : {}),
@@ -454,7 +497,10 @@ class Runner extends lifecycle_1.RunHooks {
                     ? options.context
                     : new runContext_1.RunContext(options.context), input, startingAgent, options.maxTurns === undefined
                     ? constants_1.DEFAULT_MAX_TURNS
-                    : options.maxTurns);
+                    : options.maxTurns, {
+                    historyOwnership: options.historyOwnership,
+                    modelResponseRetention: options.modelResponseRetention,
+                });
             if (isResumedState) {
                 state._agentToolInvocation = undefined;
                 if (options.maxTurns !== undefined) {
@@ -546,7 +592,7 @@ class Runner extends lifecycle_1.RunHooks {
             let persistenceCheckpoint;
             let approvedToolCheckpointCompacted = false;
             let approvedToolCheckpointRequiresLocalInputCompaction = isResumedState && hasPersistedToolOutput(state);
-            let approvedToolCheckpointModelResponseCount = state._modelResponses.length;
+            let approvedToolCheckpointModelResponseCount = state._modelResponseCount;
             let completedResultPersisted = false;
             const completeResult = (result) => {
                 completedResult = result;
@@ -554,7 +600,7 @@ class Runner extends lifecycle_1.RunHooks {
             };
             const persistNonStreamingResult = async (result) => {
                 const hasUnpersistedItems = result.newItems.length > state._currentTurnPersistedItemCount;
-                const modelResponseAdvanced = result.rawResponses.length > approvedToolCheckpointModelResponseCount;
+                const modelResponseAdvanced = result.state._modelResponseCount > approvedToolCheckpointModelResponseCount;
                 const persistenceOptions = approvedToolCheckpointRequiresLocalInputCompaction
                     ? approvedToolCheckpointCompacted &&
                         !hasUnpersistedItems &&
@@ -663,7 +709,7 @@ class Runner extends lifecycle_1.RunHooks {
                             });
                             approvedToolCheckpointCompacted = true;
                             approvedToolCheckpointModelResponseCount =
-                                approvedToolResult.rawResponses.length;
+                                approvedToolResult.state._modelResponseCount;
                         }
                         if (options.signal?.aborted) {
                             persistenceCheckpoint = new result_1.RunResult(state);
@@ -743,7 +789,9 @@ class Runner extends lifecycle_1.RunHooks {
                         sessionTurnInputUpdate?.(sessionPreparedTurnInput, preparedSandboxAgent.turnInput);
                         const artifacts = await (0, modelPreparation_1.prepareAgentArtifacts)(state, preparedSandboxAgent.executionAgent, options.toolNameCollisionPolicy);
                         const preparedCall = await this.#prepareModelCall(state, preparedSandboxAgent.executionAgent, options, artifacts, preparedSandboxAgent.turnInput, serverConversationTracker, sessionInputUpdate);
-                        (0, sessionPersistence_1.captureSessionHistoryTransactionInputItems)(options.session, state, getSessionInputForPersistence?.());
+                        if (options.historyOwnership !== 'external') {
+                            (0, sessionPersistence_1.captureSessionHistoryTransactionInputItems)(options.session, state, getSessionInputForPersistence?.());
+                        }
                         await guardrailTracker.throwIfError();
                         state._lastTurnResponse = await (0, modelRetry_1.getResponseWithRetry)(preparedCall.model, {
                             systemInstructions: preparedCall.modelInput.instructions,
@@ -772,7 +820,7 @@ class Runner extends lifecycle_1.RunHooks {
                                 allTurnItems: preparedCall.turnInput,
                             });
                         }
-                        state._modelResponses.push(state._lastTurnResponse);
+                        state.recordModelResponse(state._lastTurnResponse);
                         state._context.usage.add(state._lastTurnResponse.usage);
                         recordUsage(state._lastTurnResponse.usage);
                         state._noActiveAgentRun = false;
@@ -784,6 +832,7 @@ class Runner extends lifecycle_1.RunHooks {
                         }
                         const processedResponse = await (0, modelOutputs_1.processModelResponseAsync)(state._lastTurnResponse, state._currentAgent, preparedCall.tools, preparedCall.handoffs, state, [...preparedCall.turnInput, ...state._generatedItems], options.toolNotFoundBehavior, {
                             allowPromptSuppliedTools: preparedCall.allowPromptSuppliedTools,
+                            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
                         });
                         state._lastProcessedResponse = processedResponse;
                         await guardrailTracker.awaitCompletion();
@@ -960,7 +1009,7 @@ class Runner extends lifecycle_1.RunHooks {
         let suppressStreamInputPersistence = false;
         let approvedToolCheckpointCompacted = false;
         let approvedToolCheckpointRequiresLocalInputCompaction = isResumedState && hasPersistedToolOutput(result.state);
-        let approvedToolCheckpointModelResponseCount = result.rawResponses.length;
+        let approvedToolCheckpointModelResponseCount = result.state._modelResponseCount;
         let currentTurnSpan;
         const parentUsageRecorder = (0, usageTracking_1.getRunnerParentUsageRecorder)(this);
         const recordUsage = (usage) => {
@@ -971,7 +1020,7 @@ class Runner extends lifecycle_1.RunHooks {
         (0, usageTracking_1.setRunStateUsageRecorder)(result.state, recordUsage);
         const saveStreamResultWithCompactionOwnership = async (overrideOptions) => {
             const hasUnpersistedItems = result.newItems.length > result.state._currentTurnPersistedItemCount;
-            const modelResponseAdvanced = result.rawResponses.length > approvedToolCheckpointModelResponseCount;
+            const modelResponseAdvanced = result.state._modelResponseCount > approvedToolCheckpointModelResponseCount;
             const persistenceOptions = overrideOptions ??
                 (approvedToolCheckpointRequiresLocalInputCompaction
                     ? approvedToolCheckpointCompacted &&
@@ -1104,7 +1153,7 @@ class Runner extends lifecycle_1.RunHooks {
                         });
                         approvedToolCheckpointCompacted = true;
                         approvedToolCheckpointModelResponseCount =
-                            result.rawResponses.length;
+                            result.state._modelResponseCount;
                     }
                     // Don't reset counter here - resolveInterruptedTurn already adjusted it via rewind logic
                     // The counter will be reset when _currentTurn is incremented (starting a new turn)
@@ -1190,7 +1239,9 @@ class Runner extends lifecycle_1.RunHooks {
                     sessionTurnInputUpdate?.(sessionPreparedTurnInput, preparedSandboxAgent.turnInput);
                     const artifacts = await (0, modelPreparation_1.prepareAgentArtifacts)(result.state, preparedSandboxAgent.executionAgent, options.toolNameCollisionPolicy);
                     const preparedCall = await this.#prepareModelCall(result.state, preparedSandboxAgent.executionAgent, options, artifacts, preparedSandboxAgent.turnInput, serverConversationTracker, sessionInputUpdate);
-                    (0, sessionPersistence_1.captureSessionHistoryTransactionInputItems)(options.session, result.state, getStreamInputForPersistence?.());
+                    if (options.historyOwnership !== 'external') {
+                        (0, sessionPersistence_1.captureSessionHistoryTransactionInputItems)(options.session, result.state, getStreamInputForPersistence?.());
+                    }
                     await guardrailTracker.throwIfError();
                     // Initial request and session-persistence ordering remain unchanged.
                     // Once a logical turn is established, do not start another model
@@ -1326,9 +1377,10 @@ class Runner extends lifecycle_1.RunHooks {
                     if (serverConversationTracker) {
                         result.state.setConversationContext(serverConversationTracker.conversationId, serverConversationTracker.previousResponseId);
                     }
-                    result.state._modelResponses.push(result.state._lastTurnResponse);
+                    result.state.recordModelResponse(result.state._lastTurnResponse);
                     const processedResponse = await (0, modelOutputs_1.processModelResponseAsync)(result.state._lastTurnResponse, currentAgent, preparedCall.tools, preparedCall.handoffs, result.state, [...preparedCall.turnInput, ...result.state._generatedItems], options.toolNotFoundBehavior, {
                         allowPromptSuppliedTools: preparedCall.allowPromptSuppliedTools,
+                        resolveMissingFunctionTool: options.resolveMissingFunctionTool,
                     });
                     result.state._lastProcessedResponse = processedResponse;
                     // Record the items emitted directly from the model response so we do not
@@ -1473,7 +1525,10 @@ class Runner extends lifecycle_1.RunHooks {
                     ? options.context
                     : new runContext_1.RunContext(options.context), input, agent, options.maxTurns === undefined
                     ? constants_1.DEFAULT_MAX_TURNS
-                    : options.maxTurns);
+                    : options.maxTurns, {
+                    historyOwnership: options.historyOwnership,
+                    modelResponseRetention: options.modelResponseRetention,
+                });
             if (isResumedState) {
                 state._agentToolInvocation = undefined;
                 if (options.maxTurns !== undefined) {
@@ -1587,7 +1642,7 @@ class Runner extends lifecycle_1.RunHooks {
             !(artifacts.toolsExplicitlyProvided &&
                 artifacts.serializedTools.length === 0 &&
                 artifacts.serializedHandoffs.length === 0);
-        const { modelInput, sourceItems, persistedItems, filterApplied } = await (0, conversation_1.applyCallModelInputFilter)(state._currentAgent, options.callModelInputFilter, state._context, turnInput, systemInstructions);
+        const { modelInput, sourceItems, persistedItems, filterApplied } = await (0, conversation_1.applyCallModelInputFilter)(state._currentAgent, options.callModelInputFilter, state._context, turnInput, systemInstructions, { historyOwnership: options.historyOwnership });
         // Persist normalized clones so session history mirrors the exact model payload. An empty array
         // is intentional when a filter removes everything.
         sessionInputUpdate?.(sourceItems, persistedItems);
diff --git a/dist/run.mjs b/dist/run.mjs
index 20a7ddd8049b4404585cee04fa32eec2a8453103..8edc9b84bbd4e2f4f1c3f33bbab2fbf332ae94df 100644
--- a/dist/run.mjs
+++ b/dist/run.mjs
@@ -59,6 +59,20 @@ class LazyDefaultModelProvider {
 function isNoopTrace(trace) {
     return trace instanceof NoopTrace || trace?.traceId === NOOP_TRACE_OR_SPAN_ID;
 }
+function resolveHistoryOwnership(value) {
+    const resolved = value ?? 'sdk';
+    if (resolved !== 'sdk' && resolved !== 'external') {
+        throw new UserError("historyOwnership must be either 'sdk' or 'external'.");
+    }
+    return resolved;
+}
+function resolveModelResponseRetention(value) {
+    const resolved = value ?? 'all';
+    if (resolved !== 'all' && resolved !== 'last') {
+        throw new UserError("modelResponseRetention must be either 'all' or 'last'.");
+    }
+    return resolved;
+}
 export async function run(agent, input, options) {
     const runner = getDefaultRunner();
     if (options?.stream) {
@@ -99,9 +113,12 @@ export class Runner extends RunHooks {
             sandbox: config.sandbox,
             toolExecution: validateToolExecutionConfig(config.toolExecution),
             toolNotFoundBehavior: config.toolNotFoundBehavior ?? 'raise_error',
+            resolveMissingFunctionTool: config.resolveMissingFunctionTool,
             toolNameCollisionPolicy: validateToolNameCollisionPolicy(config.toolNameCollisionPolicy),
             sessionInputCallback: config.sessionInputCallback,
             callModelInputFilter: config.callModelInputFilter,
+            historyOwnership: resolveHistoryOwnership(config.historyOwnership),
+            modelResponseRetention: resolveModelResponseRetention(config.modelResponseRetention),
             toolErrorFormatter: config.toolErrorFormatter,
             reasoningItemIdPolicy: config.reasoningItemIdPolicy,
         };
@@ -148,6 +165,8 @@ export class Runner extends RunHooks {
         const sessionInputCallback = resolvedOptions.sessionInputCallback ?? this.config.sessionInputCallback;
         // Likewise allow callers to override callModelInputFilter on individual runs.
         const callModelInputFilter = resolvedOptions.callModelInputFilter ?? this.config.callModelInputFilter;
+        const historyOwnership = resolveHistoryOwnership(resolvedOptions.historyOwnership ?? this.config.historyOwnership);
+        const modelResponseRetention = resolveModelResponseRetention(resolvedOptions.modelResponseRetention ?? this.config.modelResponseRetention);
         // Per-run callback can override runner-level tool error formatting defaults.
         const toolErrorFormatter = resolvedOptions.toolErrorFormatter ?? this.config.toolErrorFormatter;
         const reasoningItemIdPolicy = resolvedOptions.reasoningItemIdPolicy ??
@@ -155,6 +174,7 @@ export class Runner extends RunHooks {
             this.config.reasoningItemIdPolicy;
         const toolExecution = validateToolExecutionConfig(resolvedOptions.toolExecution ?? this.config.toolExecution);
         const toolNotFoundBehavior = resolvedOptions.toolNotFoundBehavior ?? this.config.toolNotFoundBehavior;
+        const resolveMissingFunctionTool = resolvedOptions.resolveMissingFunctionTool ?? this.config.resolveMissingFunctionTool;
         const toolNameCollisionPolicy = validateToolNameCollisionPolicy(resolvedOptions.toolNameCollisionPolicy === undefined
             ? this.config.toolNameCollisionPolicy
             : resolvedOptions.toolNameCollisionPolicy);
@@ -170,10 +190,13 @@ export class Runner extends RunHooks {
             ...resolvedOptions,
             sessionInputCallback,
             callModelInputFilter,
+            historyOwnership,
+            modelResponseRetention,
             toolErrorFormatter,
             reasoningItemIdPolicy,
             toolExecution,
             toolNotFoundBehavior,
+            resolveMissingFunctionTool,
             toolNameCollisionPolicy,
             tracing: tracingConfig,
         };
@@ -195,24 +218,39 @@ export class Runner extends RunHooks {
         // When the server tracks conversation history we defer to it for previous turns so local session
         // persistence can focus solely on the new delta being generated in this process.
         const session = effectiveOptions.session;
+        if (historyOwnership === 'external' &&
+            (session !== undefined ||
+                sessionInputCallback !== undefined ||
+                serverManagesConversation)) {
+            throw new UserError('External history ownership cannot be combined with Session, sessionInputCallback, previousResponseId, or conversationId.');
+        }
         let provisionalSessionHistoryTransactionSessionId;
         let provisionalSessionHistoryTransactionInputItems;
         if (resumingFromState) {
             const resumedState = input;
-            const hadSessionBinding = resumedState._currentTurnSessionHistoryTransactionSessionId !==
-                undefined;
-            const portableInputItems = resumedState._currentTurnSessionHistoryTransactionInputItems;
+            if (historyOwnership === 'external' &&
+                (resumedState._currentTurnSessionHistoryTransactionSessionId !== undefined ||
+                    resumedState._pendingSessionHistoryTransaction !== undefined ||
+                    resumedState._pendingLegacyCompactionSessionItems !== undefined)) {
+                throw new UserError('External history ownership cannot resume RunState that is bound to SDK Session persistence.');
+            }
             resumedState.setReasoningItemIdPolicy(reasoningItemIdPolicy);
-            await prepareSessionHistoryTransactionsForRun(session, resumedState, {
-                serverManagesConversation,
-            });
-            if (!hadSessionBinding) {
-                provisionalSessionHistoryTransactionSessionId =
-                    resumedState._currentTurnSessionHistoryTransactionSessionId;
-                provisionalSessionHistoryTransactionInputItems = portableInputItems;
+            resumedState.setModelResponseRetention(modelResponseRetention);
+            if (historyOwnership !== 'external') {
+                const hadSessionBinding = resumedState._currentTurnSessionHistoryTransactionSessionId !==
+                    undefined;
+                const portableInputItems = resumedState._currentTurnSessionHistoryTransactionInputItems;
+                await prepareSessionHistoryTransactionsForRun(session, resumedState, {
+                    serverManagesConversation,
+                });
+                if (!hadSessionBinding) {
+                    provisionalSessionHistoryTransactionSessionId =
+                        resumedState._currentTurnSessionHistoryTransactionSessionId;
+                    provisionalSessionHistoryTransactionInputItems = portableInputItems;
+                }
             }
         }
-        const sessionPersistence = createSessionPersistenceTracker({
+        const sessionPersistence = historyOwnership === 'external' ? undefined : createSessionPersistenceTracker({
             session,
             hasCallModelInputFilter,
             persistInput: saveStreamInputToSession,
@@ -374,11 +412,13 @@ export class Runner extends RunHooks {
         const hasSandboxOverride = typeof options.sandbox !== 'undefined';
         const hasToolExecutionOverride = typeof options.toolExecution !== 'undefined';
         const hasToolNotFoundBehaviorOverride = typeof options.toolNotFoundBehavior !== 'undefined';
+        const hasResolveMissingFunctionToolOverride = typeof options.resolveMissingFunctionTool !== 'undefined';
         const hasToolNameCollisionPolicyOverride = typeof options.toolNameCollisionPolicy !== 'undefined';
         const hasTracingOverride = typeof options.tracing !== 'undefined';
         if (!hasSandboxOverride &&
             !hasToolExecutionOverride &&
             !hasToolNotFoundBehaviorOverride &&
+            !hasResolveMissingFunctionToolOverride &&
             !hasToolNameCollisionPolicyOverride &&
             !hasTracingOverride) {
             return this.config;
@@ -392,6 +432,9 @@ export class Runner extends RunHooks {
             ...(hasToolNotFoundBehaviorOverride
                 ? { toolNotFoundBehavior: options.toolNotFoundBehavior }
                 : {}),
+            ...(hasResolveMissingFunctionToolOverride
+                ? { resolveMissingFunctionTool: options.resolveMissingFunctionTool }
+                : {}),
             ...(hasToolNameCollisionPolicyOverride
                 ? { toolNameCollisionPolicy: options.toolNameCollisionPolicy }
                 : {}),
@@ -414,7 +457,10 @@ export class Runner extends RunHooks {
                     ? options.context
                     : new RunContext(options.context), input, startingAgent, options.maxTurns === undefined
                     ? DEFAULT_MAX_TURNS
-                    : options.maxTurns);
+                    : options.maxTurns, {
+                    historyOwnership: options.historyOwnership,
+                    modelResponseRetention: options.modelResponseRetention,
+                });
             if (isResumedState) {
                 state._agentToolInvocation = undefined;
                 if (options.maxTurns !== undefined) {
@@ -506,7 +552,7 @@ export class Runner extends RunHooks {
             let persistenceCheckpoint;
             let approvedToolCheckpointCompacted = false;
             let approvedToolCheckpointRequiresLocalInputCompaction = isResumedState && hasPersistedToolOutput(state);
-            let approvedToolCheckpointModelResponseCount = state._modelResponses.length;
+            let approvedToolCheckpointModelResponseCount = state._modelResponseCount;
             let completedResultPersisted = false;
             const completeResult = (result) => {
                 completedResult = result;
@@ -514,7 +560,7 @@ export class Runner extends RunHooks {
             };
             const persistNonStreamingResult = async (result) => {
                 const hasUnpersistedItems = result.newItems.length > state._currentTurnPersistedItemCount;
-                const modelResponseAdvanced = result.rawResponses.length > approvedToolCheckpointModelResponseCount;
+                const modelResponseAdvanced = result.state._modelResponseCount > approvedToolCheckpointModelResponseCount;
                 const persistenceOptions = approvedToolCheckpointRequiresLocalInputCompaction
                     ? approvedToolCheckpointCompacted &&
                         !hasUnpersistedItems &&
@@ -623,7 +669,7 @@ export class Runner extends RunHooks {
                             });
                             approvedToolCheckpointCompacted = true;
                             approvedToolCheckpointModelResponseCount =
-                                approvedToolResult.rawResponses.length;
+                                approvedToolResult.state._modelResponseCount;
                         }
                         if (options.signal?.aborted) {
                             persistenceCheckpoint = new RunResult(state);
@@ -703,7 +749,9 @@ export class Runner extends RunHooks {
                         sessionTurnInputUpdate?.(sessionPreparedTurnInput, preparedSandboxAgent.turnInput);
                         const artifacts = await prepareAgentArtifacts(state, preparedSandboxAgent.executionAgent, options.toolNameCollisionPolicy);
                         const preparedCall = await this.#prepareModelCall(state, preparedSandboxAgent.executionAgent, options, artifacts, preparedSandboxAgent.turnInput, serverConversationTracker, sessionInputUpdate);
-                        captureSessionHistoryTransactionInputItems(options.session, state, getSessionInputForPersistence?.());
+                        if (options.historyOwnership !== 'external') {
+                            captureSessionHistoryTransactionInputItems(options.session, state, getSessionInputForPersistence?.());
+                        }
                         await guardrailTracker.throwIfError();
                         state._lastTurnResponse = await getResponseWithRetry(preparedCall.model, {
                             systemInstructions: preparedCall.modelInput.instructions,
@@ -732,7 +780,7 @@ export class Runner extends RunHooks {
                                 allTurnItems: preparedCall.turnInput,
                             });
                         }
-                        state._modelResponses.push(state._lastTurnResponse);
+                        state.recordModelResponse(state._lastTurnResponse);
                         state._context.usage.add(state._lastTurnResponse.usage);
                         recordUsage(state._lastTurnResponse.usage);
                         state._noActiveAgentRun = false;
@@ -744,6 +792,7 @@ export class Runner extends RunHooks {
                         }
                         const processedResponse = await processModelResponseAsync(state._lastTurnResponse, state._currentAgent, preparedCall.tools, preparedCall.handoffs, state, [...preparedCall.turnInput, ...state._generatedItems], options.toolNotFoundBehavior, {
                             allowPromptSuppliedTools: preparedCall.allowPromptSuppliedTools,
+                            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
                         });
                         state._lastProcessedResponse = processedResponse;
                         await guardrailTracker.awaitCompletion();
@@ -920,7 +969,7 @@ export class Runner extends RunHooks {
         let suppressStreamInputPersistence = false;
         let approvedToolCheckpointCompacted = false;
         let approvedToolCheckpointRequiresLocalInputCompaction = isResumedState && hasPersistedToolOutput(result.state);
-        let approvedToolCheckpointModelResponseCount = result.rawResponses.length;
+        let approvedToolCheckpointModelResponseCount = result.state._modelResponseCount;
         let currentTurnSpan;
         const parentUsageRecorder = getRunnerParentUsageRecorder(this);
         const recordUsage = (usage) => {
@@ -931,7 +980,7 @@ export class Runner extends RunHooks {
         setRunStateUsageRecorder(result.state, recordUsage);
         const saveStreamResultWithCompactionOwnership = async (overrideOptions) => {
             const hasUnpersistedItems = result.newItems.length > result.state._currentTurnPersistedItemCount;
-            const modelResponseAdvanced = result.rawResponses.length > approvedToolCheckpointModelResponseCount;
+            const modelResponseAdvanced = result.state._modelResponseCount > approvedToolCheckpointModelResponseCount;
             const persistenceOptions = overrideOptions ??
                 (approvedToolCheckpointRequiresLocalInputCompaction
                     ? approvedToolCheckpointCompacted &&
@@ -1064,7 +1113,7 @@ export class Runner extends RunHooks {
                         });
                         approvedToolCheckpointCompacted = true;
                         approvedToolCheckpointModelResponseCount =
-                            result.rawResponses.length;
+                            result.state._modelResponseCount;
                     }
                     // Don't reset counter here - resolveInterruptedTurn already adjusted it via rewind logic
                     // The counter will be reset when _currentTurn is incremented (starting a new turn)
@@ -1150,7 +1199,9 @@ export class Runner extends RunHooks {
                     sessionTurnInputUpdate?.(sessionPreparedTurnInput, preparedSandboxAgent.turnInput);
                     const artifacts = await prepareAgentArtifacts(result.state, preparedSandboxAgent.executionAgent, options.toolNameCollisionPolicy);
                     const preparedCall = await this.#prepareModelCall(result.state, preparedSandboxAgent.executionAgent, options, artifacts, preparedSandboxAgent.turnInput, serverConversationTracker, sessionInputUpdate);
-                    captureSessionHistoryTransactionInputItems(options.session, result.state, getStreamInputForPersistence?.());
+                    if (options.historyOwnership !== 'external') {
+                        captureSessionHistoryTransactionInputItems(options.session, result.state, getStreamInputForPersistence?.());
+                    }
                     await guardrailTracker.throwIfError();
                     // Initial request and session-persistence ordering remain unchanged.
                     // Once a logical turn is established, do not start another model
@@ -1286,9 +1337,10 @@ export class Runner extends RunHooks {
                     if (serverConversationTracker) {
                         result.state.setConversationContext(serverConversationTracker.conversationId, serverConversationTracker.previousResponseId);
                     }
-                    result.state._modelResponses.push(result.state._lastTurnResponse);
+                    result.state.recordModelResponse(result.state._lastTurnResponse);
                     const processedResponse = await processModelResponseAsync(result.state._lastTurnResponse, currentAgent, preparedCall.tools, preparedCall.handoffs, result.state, [...preparedCall.turnInput, ...result.state._generatedItems], options.toolNotFoundBehavior, {
                         allowPromptSuppliedTools: preparedCall.allowPromptSuppliedTools,
+                        resolveMissingFunctionTool: options.resolveMissingFunctionTool,
                     });
                     result.state._lastProcessedResponse = processedResponse;
                     // Record the items emitted directly from the model response so we do not
@@ -1433,7 +1485,10 @@ export class Runner extends RunHooks {
                     ? options.context
                     : new RunContext(options.context), input, agent, options.maxTurns === undefined
                     ? DEFAULT_MAX_TURNS
-                    : options.maxTurns);
+                    : options.maxTurns, {
+                    historyOwnership: options.historyOwnership,
+                    modelResponseRetention: options.modelResponseRetention,
+                });
             if (isResumedState) {
                 state._agentToolInvocation = undefined;
                 if (options.maxTurns !== undefined) {
@@ -1547,7 +1602,7 @@ export class Runner extends RunHooks {
             !(artifacts.toolsExplicitlyProvided &&
                 artifacts.serializedTools.length === 0 &&
                 artifacts.serializedHandoffs.length === 0);
-        const { modelInput, sourceItems, persistedItems, filterApplied } = await applyCallModelInputFilter(state._currentAgent, options.callModelInputFilter, state._context, turnInput, systemInstructions);
+        const { modelInput, sourceItems, persistedItems, filterApplied } = await applyCallModelInputFilter(state._currentAgent, options.callModelInputFilter, state._context, turnInput, systemInstructions, { historyOwnership: options.historyOwnership });
         // Persist normalized clones so session history mirrors the exact model payload. An empty array
         // is intentional when a filter removes everything.
         sessionInputUpdate?.(sourceItems, persistedItems);
diff --git a/dist/runState.d.ts b/dist/runState.d.ts
index 9bbf67c71a80f110ab841d4553397633a6df4faf..b48ffd840dbe24d03509cd995cf59db504028641 100644
--- a/dist/runState.d.ts
+++ b/dist/runState.d.ts
@@ -16,6 +16,7 @@ import { AgentInputItem, UnknownContext } from './types';
 import type { InputGuardrailResult, OutputGuardrailResult } from './guardrail';
 import type { ToolInputGuardrailResult, ToolOutputGuardrailResult } from './toolGuardrail';
 import { Tool } from './tool';
+import type { ResolveMissingFunctionTool } from './run';
 import type { AgentToolInvocation } from './agentToolInvocation';
 /**
  * The schema version of the serialized run state. This is used to ensure that the serialized
@@ -53,6 +54,22 @@ import type { AgentToolInvocation } from './agentToolInvocation';
  */
 export declare const CURRENT_SCHEMA_VERSION: "1.17";
 type ContextOverrideStrategy = 'merge' | 'replace';
+export type ClientToolSearchRehydration = 'execute' | 'preserve_history';
+export type RunStateDeserializationOptions = {
+    /**
+     * Controls how serialized client tool_search outputs recover executable tools.
+     * `execute` preserves the default SDK behavior and reruns each custom search
+     * callback. `preserve_history` never reruns historical callbacks and instead
+     * rebinds matching tools from the current configured agent catalog. Historical
+     * tools that are no longer configured remain history only.
+     */
+    clientToolSearchRehydration?: ClientToolSearchRehydration;
+    /**
+     * Same resolver as live processModelResponse. Used when a pending function
+     * call is not on the configured Agent list after catalog preparation.
+     */
+    resolveMissingFunctionTool?: ResolveMissingFunctionTool;
+};
 declare const pendingSessionHistoryTransactionSchema: z.ZodObject<{
     operationId: z.ZodString;
     transactionKind: z.ZodEnum<{
@@ -4224,6 +4241,10 @@ export declare class RunState<TContext, TAgent extends Agent<any, any>> {
      * Responses from the model so far.
      */
     _modelResponses: ModelResponse[];
+    /** Monotonic response count, independent of raw-response retention. */
+    _modelResponseCount: number;
+    /** Active raw-response retention policy for this run. */
+    _modelResponseRetention: 'all' | 'last';
     /**
      * Conversation identifier when the server manages conversation history.
      */
@@ -4383,7 +4404,10 @@ export declare class RunState<TContext, TAgent extends Agent<any, any>> {
      * Persisted sandbox session metadata for sandbox-agent resume.
      */
     _sandbox: z.infer<typeof sandboxStateSchema> | undefined;
-    constructor(context: RunContext<TContext>, originalInput: string | AgentInputItem[], startingAgent: TAgent, maxTurns: number | null);
+    constructor(context: RunContext<TContext>, originalInput: string | AgentInputItem[], startingAgent: TAgent, maxTurns: number | null, options?: {
+        historyOwnership?: 'sdk' | 'external';
+        modelResponseRetention?: 'all' | 'last';
+    });
     /**
      * Updates server-managed conversation identifiers as a single operation.
      */
@@ -4392,6 +4416,8 @@ export declare class RunState<TContext, TAgent extends Agent<any, any>> {
      * Updates runtime options for converting run items into turn input.
      */
     setReasoningItemIdPolicy(policy?: ReasoningItemIdPolicy): void;
+    setModelResponseRetention(retention: 'all' | 'last'): void;
+    recordModelResponse(response: ModelResponse): void;
     /**
      * Updates the agent span associated with the current run.
      */
@@ -4511,8 +4537,8 @@ export declare class RunState<TContext, TAgent extends Agent<any, any>> {
      * This method is used to deserialize a run state from a string that was serialized using the
      * `toString` method.
      */
-    static fromString<TContext, TAgent extends Agent<any, any>>(initialAgent: TAgent, str: string): Promise<RunState<TContext, TAgent>>;
-    static fromStringWithContext<TContext, TAgent extends Agent<any, any>>(initialAgent: TAgent, str: string, context: RunContext<TContext>, options?: {
+    static fromString<TContext, TAgent extends Agent<any, any>>(initialAgent: TAgent, str: string, options?: RunStateDeserializationOptions): Promise<RunState<TContext, TAgent>>;
+    static fromStringWithContext<TContext, TAgent extends Agent<any, any>>(initialAgent: TAgent, str: string, context: RunContext<TContext>, options?: RunStateDeserializationOptions & {
         contextStrategy?: ContextOverrideStrategy;
     }): Promise<RunState<TContext, TAgent>>;
 }
diff --git a/dist/runState.js b/dist/runState.js
index deb9514cf094157893778976e2ef03e7fefe0965..53c1504545a208b28503e6648d9b768a3c49f6ad 100644
--- a/dist/runState.js
+++ b/dist/runState.js
@@ -500,6 +500,15 @@ class RunState {
      * Responses from the model so far.
      */
     _modelResponses;
+    /**
+     * Monotonic number of model responses observed, independent of retention.
+     */
+    _modelResponseCount;
+    /**
+     * Controls whether raw response history retains every response or only the
+     * latest response. Generated items and aggregate usage are unaffected.
+     */
+    _modelResponseRetention;
     /**
      * Conversation identifier when the server manages conversation history.
      */
@@ -661,11 +670,17 @@ class RunState {
      * Persisted sandbox session metadata for sandbox-agent resume.
      */
     _sandbox = undefined;
-    constructor(context, originalInput, startingAgent, maxTurns) {
+    constructor(context, originalInput, startingAgent, maxTurns, options = {}) {
         this._context = context;
         this._agentToolInvocation = undefined;
-        this._originalInput = structuredClone(originalInput);
+        this._originalInput = options.historyOwnership === 'external' && Array.isArray(originalInput)
+            ? [...originalInput]
+            : options.historyOwnership === 'external'
+                ? originalInput
+                : structuredClone(originalInput);
         this._modelResponses = [];
+        this._modelResponseCount = 0;
+        this._modelResponseRetention = options.modelResponseRetention ?? 'all';
         this._currentAgentSpan = undefined;
         this._currentAgent = startingAgent;
         this.#startingAgent = startingAgent;
@@ -706,6 +721,26 @@ class RunState {
     setReasoningItemIdPolicy(policy) {
         this._reasoningItemIdPolicy = policy;
     }
+    setModelResponseRetention(retention) {
+        this._modelResponseRetention = retention;
+        if (retention === 'last' && this._modelResponses.length > 1) {
+            this._modelResponses.splice(0, this._modelResponses.length - 1);
+        }
+    }
+    recordModelResponse(response) {
+        this._modelResponseCount += 1;
+        if (this._modelResponseRetention === 'last') {
+            if (this._modelResponses.length === 0) {
+                this._modelResponses.push(response);
+            }
+            else {
+                this._modelResponses[0] = response;
+                this._modelResponses.length = 1;
+            }
+            return;
+        }
+        this._modelResponses.push(response);
+    }
     /**
      * Updates the agent span associated with the current run.
      */
@@ -1040,13 +1075,18 @@ class RunState {
      * This method is used to deserialize a run state from a string that was serialized using the
      * `toString` method.
      */
-    static async fromString(initialAgent, str) {
-        return buildRunStateFromString(initialAgent, str);
+    static async fromString(initialAgent, str, options = {}) {
+        return buildRunStateFromString(initialAgent, str, {
+            clientToolSearchRehydration: options.clientToolSearchRehydration,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
+        });
     }
     static async fromStringWithContext(initialAgent, str, context, options = {}) {
         return buildRunStateFromString(initialAgent, str, {
             contextOverride: context,
             contextStrategy: options.contextStrategy,
+            clientToolSearchRehydration: options.clientToolSearchRehydration,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
         });
     }
 }
@@ -1837,6 +1877,19 @@ function selectSerializedRuntimeTools(args) {
     const callId = (0, toolSearch_1.resolveToolSearchCallId)(toolSearchCall);
     throw new errors_1.UserError(`RunState cannot resume custom client tool_search call ${callId} for agent ${agent.name} because the registered execute callback returned runtime tools [${formatRuntimeToolKeys(actualRuntimeToolKeys)}] but the serialized state expects [${formatRuntimeToolKeys(expectedRuntimeToolKeys)}].`);
 }
+function selectConfiguredRuntimeTools(expectedRuntimeToolKeys, availableTools) {
+    const availableToolsByKey = new Map();
+    for (const tool of availableTools) {
+        const runtimeToolKey = (0, tool_1.getToolSearchRuntimeRoutingKey)(tool);
+        if (runtimeToolKey) {
+            availableToolsByKey.set(runtimeToolKey, tool);
+        }
+    }
+    return [...expectedRuntimeToolKeys].flatMap((runtimeToolKey) => {
+        const tool = availableToolsByKey.get(runtimeToolKey);
+        return tool ? [tool] : [];
+    });
+}
 async function getConfiguredAgentTools(args) {
     const { agent, context, configuredToolsByAgent } = args;
     const existing = configuredToolsByAgent.get(agent);
@@ -2074,6 +2127,11 @@ function assertDeferredFunctionCallProvenance(args) {
     }
 }
 async function rehydrateToolSearchRuntimeTools(state, options) {
+    const clientToolSearchRehydration = options.clientToolSearchRehydration ?? 'execute';
+    if (clientToolSearchRehydration !== 'execute' &&
+        clientToolSearchRehydration !== 'preserve_history') {
+        throw new errors_1.UserError("clientToolSearchRehydration must be either 'execute' or 'preserve_history'.");
+    }
     const deferredFunctionCallExpectations = assertUnambiguousFunctionCallIds(state, options.agentMap, options.serializedProcessedResponse);
     const configuredToolsByAgent = new Map();
     const capabilitySnapshotsByAgent = new Map();
@@ -2204,6 +2262,14 @@ async function rehydrateToolSearchRuntimeTools(state, options) {
         const pendingCall = occurrencesByOutput.get(item).pendingCall;
         const toolSearchTool = (0, toolSearch_2.getClientToolSearchHelper)(snapshot.availableTools);
         const hasCustomExecutor = Boolean(toolSearchTool && (0, tool_1.getClientToolSearchExecutor)(toolSearchTool));
+        if (clientToolSearchRehydration === 'preserve_history') {
+            rehydrationRecords.set(item, {
+                pendingCall,
+                expectedRuntimeToolKeys,
+                runtimeTools: selectConfiguredRuntimeTools(expectedRuntimeToolKeys, snapshot.availableTools),
+            });
+            continue;
+        }
         if (expectedRuntimeToolKeys.size === 0) {
             rehydrationRecords.set(item, {
                 pendingCall,
@@ -2408,6 +2474,8 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
     state._currentStep = stateJson.currentStep;
     state._originalInput = stateJson.originalInput;
     state._modelResponses = stateJson.modelResponses.map(deserializeModelResponse);
+    state._modelResponseCount = state._modelResponses.length;
+    state._modelResponseRetention = 'all';
     state._lastTurnResponse = stateJson.lastModelResponse
         ? deserializeModelResponse(stateJson.lastModelResponse)
         : undefined;
@@ -2436,6 +2504,7 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
     const capabilitySnapshotsByAgent = await rehydrateToolSearchRuntimeTools(state, {
         agentMap,
         schemaVersion,
+        clientToolSearchRehydration: options.clientToolSearchRehydration,
         serializedProcessedResponse: stateJson.lastProcessedResponse,
         prepareCurrentAgentForLegacyApprovals: !schemaVersionSupportsV116State(schemaVersion) &&
             shouldRestoreSerializedContext &&
@@ -2447,6 +2516,7 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
             executionTools: currentCapabilitySnapshot.availableTools,
             executionHandoffs: currentCapabilitySnapshot.handoffs,
             executionFunctionToolsByCallId: currentCapabilitySnapshot.functionToolsByCallId,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
         })
         : undefined;
     restorePendingAgentToolRunAliases(state, stateJson.pendingAgentToolRunAliases ?? {});
@@ -3066,9 +3136,27 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                     (0, toolIdentity_1.getFunctionToolStateKeyForCall)(functionCall.toolCall) ?? functionCall.tool.name,
                 ]);
             }
-            const resolvedTool = exactRuntimeTool ??
+            let resolvedTool = exactRuntimeTool ??
                 (0, toolIdentity_1.resolveFunctionToolCall)(functionCall.toolCall, tools) ??
-                (0, toolRehydration_1.getSerializedFunctionToolPlaceholder)({
+                null;
+            let boundViaMissingResolver = false;
+            if (!resolvedTool && options.resolveMissingFunctionTool) {
+                const missing = await options.resolveMissingFunctionTool({
+                    name: toolIdentity,
+                    agent: currentAgent,
+                    toolCall: functionCall.toolCall,
+                });
+                if (missing && missing.type === 'function') {
+                    const runtimeFunctionMap = (0, toolIdentity_1.buildFunctionToolLookupMap)([missing]);
+                    for (const [key, mapped] of runtimeFunctionMap) {
+                        tools.set(key, mapped);
+                    }
+                    resolvedTool = missing;
+                    boundViaMissingResolver = true;
+                }
+            }
+            if (!resolvedTool) {
+                resolvedTool = (0, toolRehydration_1.getSerializedFunctionToolPlaceholder)({
                     agent: currentAgent,
                     baseAgentTools,
                     serializedTool: functionCall.tool,
@@ -3076,6 +3164,7 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                     toolIdentity,
                     allowSerializedExecutionToolPlaceholder,
                 });
+            }
             if (!resolvedTool) {
                 throw new errors_1.UserError(`Tool ${toolIdentity} not found`);
             }
@@ -3086,7 +3175,8 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                 availableFunctionTools: [
                     ...new Set([...tools.values(), resolvedTool]),
                 ],
-                preserveToolOnExecutionRehydration: Boolean(exactRuntimeTool),
+                preserveToolOnExecutionRehydration: Boolean(exactRuntimeTool) ||
+                    boundViaMissingResolver,
             });
         })),
         functionToolsNotFound: serializedProcessedResponse.functionToolsNotFound ?? [],
diff --git a/dist/runState.mjs b/dist/runState.mjs
index fcba32246b7f942a5777aa38ae4663fb2c1a9df4..16f39eb40400119728c86cb32bcf013b25f43496 100644
--- a/dist/runState.mjs
+++ b/dist/runState.mjs
@@ -455,6 +455,15 @@ export class RunState {
      * Responses from the model so far.
      */
     _modelResponses;
+    /**
+     * Monotonic number of model responses observed, independent of retention.
+     */
+    _modelResponseCount;
+    /**
+     * Controls whether raw response history retains every response or only the
+     * latest response. Generated items and aggregate usage are unaffected.
+     */
+    _modelResponseRetention;
     /**
      * Conversation identifier when the server manages conversation history.
      */
@@ -616,11 +625,17 @@ export class RunState {
      * Persisted sandbox session metadata for sandbox-agent resume.
      */
     _sandbox = undefined;
-    constructor(context, originalInput, startingAgent, maxTurns) {
+    constructor(context, originalInput, startingAgent, maxTurns, options = {}) {
         this._context = context;
         this._agentToolInvocation = undefined;
-        this._originalInput = structuredClone(originalInput);
+        this._originalInput = options.historyOwnership === 'external' && Array.isArray(originalInput)
+            ? [...originalInput]
+            : options.historyOwnership === 'external'
+                ? originalInput
+                : structuredClone(originalInput);
         this._modelResponses = [];
+        this._modelResponseCount = 0;
+        this._modelResponseRetention = options.modelResponseRetention ?? 'all';
         this._currentAgentSpan = undefined;
         this._currentAgent = startingAgent;
         this.#startingAgent = startingAgent;
@@ -661,6 +676,26 @@ export class RunState {
     setReasoningItemIdPolicy(policy) {
         this._reasoningItemIdPolicy = policy;
     }
+    setModelResponseRetention(retention) {
+        this._modelResponseRetention = retention;
+        if (retention === 'last' && this._modelResponses.length > 1) {
+            this._modelResponses.splice(0, this._modelResponses.length - 1);
+        }
+    }
+    recordModelResponse(response) {
+        this._modelResponseCount += 1;
+        if (this._modelResponseRetention === 'last') {
+            if (this._modelResponses.length === 0) {
+                this._modelResponses.push(response);
+            }
+            else {
+                this._modelResponses[0] = response;
+                this._modelResponses.length = 1;
+            }
+            return;
+        }
+        this._modelResponses.push(response);
+    }
     /**
      * Updates the agent span associated with the current run.
      */
@@ -995,13 +1030,18 @@ export class RunState {
      * This method is used to deserialize a run state from a string that was serialized using the
      * `toString` method.
      */
-    static async fromString(initialAgent, str) {
-        return buildRunStateFromString(initialAgent, str);
+    static async fromString(initialAgent, str, options = {}) {
+        return buildRunStateFromString(initialAgent, str, {
+            clientToolSearchRehydration: options.clientToolSearchRehydration,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
+        });
     }
     static async fromStringWithContext(initialAgent, str, context, options = {}) {
         return buildRunStateFromString(initialAgent, str, {
             contextOverride: context,
             contextStrategy: options.contextStrategy,
+            clientToolSearchRehydration: options.clientToolSearchRehydration,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
         });
     }
 }
@@ -1791,6 +1831,19 @@ function selectSerializedRuntimeTools(args) {
     const callId = resolveToolSearchCallId(toolSearchCall);
     throw new UserError(`RunState cannot resume custom client tool_search call ${callId} for agent ${agent.name} because the registered execute callback returned runtime tools [${formatRuntimeToolKeys(actualRuntimeToolKeys)}] but the serialized state expects [${formatRuntimeToolKeys(expectedRuntimeToolKeys)}].`);
 }
+function selectConfiguredRuntimeTools(expectedRuntimeToolKeys, availableTools) {
+    const availableToolsByKey = new Map();
+    for (const tool of availableTools) {
+        const runtimeToolKey = getToolSearchRuntimeRoutingKey(tool);
+        if (runtimeToolKey) {
+            availableToolsByKey.set(runtimeToolKey, tool);
+        }
+    }
+    return [...expectedRuntimeToolKeys].flatMap((runtimeToolKey) => {
+        const tool = availableToolsByKey.get(runtimeToolKey);
+        return tool ? [tool] : [];
+    });
+}
 async function getConfiguredAgentTools(args) {
     const { agent, context, configuredToolsByAgent } = args;
     const existing = configuredToolsByAgent.get(agent);
@@ -2028,6 +2081,11 @@ function assertDeferredFunctionCallProvenance(args) {
     }
 }
 async function rehydrateToolSearchRuntimeTools(state, options) {
+    const clientToolSearchRehydration = options.clientToolSearchRehydration ?? 'execute';
+    if (clientToolSearchRehydration !== 'execute' &&
+        clientToolSearchRehydration !== 'preserve_history') {
+        throw new UserError("clientToolSearchRehydration must be either 'execute' or 'preserve_history'.");
+    }
     const deferredFunctionCallExpectations = assertUnambiguousFunctionCallIds(state, options.agentMap, options.serializedProcessedResponse);
     const configuredToolsByAgent = new Map();
     const capabilitySnapshotsByAgent = new Map();
@@ -2158,6 +2216,14 @@ async function rehydrateToolSearchRuntimeTools(state, options) {
         const pendingCall = occurrencesByOutput.get(item).pendingCall;
         const toolSearchTool = getClientToolSearchHelper(snapshot.availableTools);
         const hasCustomExecutor = Boolean(toolSearchTool && getClientToolSearchExecutor(toolSearchTool));
+        if (clientToolSearchRehydration === 'preserve_history') {
+            rehydrationRecords.set(item, {
+                pendingCall,
+                expectedRuntimeToolKeys,
+                runtimeTools: selectConfiguredRuntimeTools(expectedRuntimeToolKeys, snapshot.availableTools),
+            });
+            continue;
+        }
         if (expectedRuntimeToolKeys.size === 0) {
             rehydrationRecords.set(item, {
                 pendingCall,
@@ -2362,6 +2428,8 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
     state._currentStep = stateJson.currentStep;
     state._originalInput = stateJson.originalInput;
     state._modelResponses = stateJson.modelResponses.map(deserializeModelResponse);
+    state._modelResponseCount = state._modelResponses.length;
+    state._modelResponseRetention = 'all';
     state._lastTurnResponse = stateJson.lastModelResponse
         ? deserializeModelResponse(stateJson.lastModelResponse)
         : undefined;
@@ -2390,6 +2458,7 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
     const capabilitySnapshotsByAgent = await rehydrateToolSearchRuntimeTools(state, {
         agentMap,
         schemaVersion,
+        clientToolSearchRehydration: options.clientToolSearchRehydration,
         serializedProcessedResponse: stateJson.lastProcessedResponse,
         prepareCurrentAgentForLegacyApprovals: !schemaVersionSupportsV116State(schemaVersion) &&
             shouldRestoreSerializedContext &&
@@ -2401,6 +2470,7 @@ async function buildRunStateFromJson(initialAgent, stateJson, options = {}) {
             executionTools: currentCapabilitySnapshot.availableTools,
             executionHandoffs: currentCapabilitySnapshot.handoffs,
             executionFunctionToolsByCallId: currentCapabilitySnapshot.functionToolsByCallId,
+            resolveMissingFunctionTool: options.resolveMissingFunctionTool,
         })
         : undefined;
     restorePendingAgentToolRunAliases(state, stateJson.pendingAgentToolRunAliases ?? {});
@@ -3020,9 +3090,27 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                     getFunctionToolStateKeyForCall(functionCall.toolCall) ?? functionCall.tool.name,
                 ]);
             }
-            const resolvedTool = exactRuntimeTool ??
+            let resolvedTool = exactRuntimeTool ??
                 resolveFunctionToolCall(functionCall.toolCall, tools) ??
-                getSerializedFunctionToolPlaceholder({
+                null;
+            let boundViaMissingResolver = false;
+            if (!resolvedTool && options.resolveMissingFunctionTool) {
+                const missing = await options.resolveMissingFunctionTool({
+                    name: toolIdentity,
+                    agent: currentAgent,
+                    toolCall: functionCall.toolCall,
+                });
+                if (missing && missing.type === 'function') {
+                    const runtimeFunctionMap = buildFunctionToolLookupMap([missing]);
+                    for (const [key, mapped] of runtimeFunctionMap) {
+                        tools.set(key, mapped);
+                    }
+                    resolvedTool = missing;
+                    boundViaMissingResolver = true;
+                }
+            }
+            if (!resolvedTool) {
+                resolvedTool = getSerializedFunctionToolPlaceholder({
                     agent: currentAgent,
                     baseAgentTools,
                     serializedTool: functionCall.tool,
@@ -3030,6 +3118,7 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                     toolIdentity,
                     allowSerializedExecutionToolPlaceholder,
                 });
+            }
             if (!resolvedTool) {
                 throw new UserError(`Tool ${toolIdentity} not found`);
             }
@@ -3040,7 +3129,8 @@ async function deserializeProcessedResponse(agentMap, state, serializedProcessed
                 availableFunctionTools: [
                     ...new Set([...tools.values(), resolvedTool]),
                 ],
-                preserveToolOnExecutionRehydration: Boolean(exactRuntimeTool),
+                preserveToolOnExecutionRehydration: Boolean(exactRuntimeTool) ||
+                    boundViaMissingResolver,
             });
         })),
         functionToolsNotFound: serializedProcessedResponse.functionToolsNotFound ?? [],
diff --git a/dist/runner/conversation.d.ts b/dist/runner/conversation.d.ts
index 5e864ffbc632d2a0fff910297a323cee33111bb5..378603366576b5f940efb2b8b5dbd30c3f0c441a 100644
--- a/dist/runner/conversation.d.ts
+++ b/dist/runner/conversation.d.ts
@@ -34,11 +34,14 @@ export type FilterApplicationResult = {
     persistedItems: AgentInputItem[];
     filterApplied: boolean;
 };
+export type CallModelInputApplicationOptions = {
+    historyOwnership?: 'sdk' | 'external';
+};
 /**
  * Applies the optional callModelInputFilter and returns the filtered input alongside the original
  * items so downstream tracking and session persistence stay in sync with what the model saw.
  */
-export declare function applyCallModelInputFilter<TContext>(agent: Agent<TContext, AgentOutputType>, callModelInputFilter: CallModelInputFilter<any> | undefined, context: RunContext<TContext>, inputItems: AgentInputItem[], systemInstructions: string | undefined): Promise<FilterApplicationResult>;
+export declare function applyCallModelInputFilter<TContext>(agent: Agent<TContext, AgentOutputType>, callModelInputFilter: CallModelInputFilter<any> | undefined, context: RunContext<TContext>, inputItems: AgentInputItem[], systemInstructions: string | undefined, options?: CallModelInputApplicationOptions): Promise<FilterApplicationResult>;
 /**
  * Tracks which items have already been sent to or received from the Responses API when the caller
  * supplies `conversationId`/`previousResponseId`. This ensures we only send the delta each turn.
diff --git a/dist/runner/conversation.js b/dist/runner/conversation.js
index d46c7789ca38cd08b3ac8ee635298ab848335c6a..7dc6c44ab2ea56751aabbb939fe6891b2c340f3e 100644
--- a/dist/runner/conversation.js
+++ b/dist/runner/conversation.js
@@ -12,7 +12,7 @@ Object.defineProperty(exports, "getTurnInput", { enumerable: true, get: function
  * Applies the optional callModelInputFilter and returns the filtered input alongside the original
  * items so downstream tracking and session persistence stay in sync with what the model saw.
  */
-async function applyCallModelInputFilter(agent, callModelInputFilter, context, inputItems, systemInstructions) {
+async function applyCallModelInputFilter(agent, callModelInputFilter, context, inputItems, systemInstructions, options = {}) {
     const cloneInputItems = (items, map) => items.map((item) => {
         const cloned = structuredClone(item);
         if (map && cloned && typeof cloned === 'object') {
@@ -22,15 +22,25 @@ async function applyCallModelInputFilter(agent, callModelInputFilter, context, i
     });
     // Record the relationship between the cloned array passed to filters and the original inputs.
     const cloneMap = new WeakMap();
-    // Always create a deep copy so downstream mutations inside filters cannot affect
-    // the cached turn state.
-    const clonedBaseInput = cloneInputItems(inputItems, cloneMap);
+    // Application-owned history is a borrowed immutable view: copy only its array
+    // boundary. SDK ownership preserves the defensive deep-clone behavior.
+    const clonedBaseInput = options.historyOwnership === 'external'
+        ? [...inputItems]
+        : cloneInputItems(inputItems, cloneMap);
     const base = {
         input: clonedBaseInput,
         instructions: systemInstructions,
     };
     if (!callModelInputFilter) {
         const normalizedInput = (0, items_1.deduplicateAgentInputItemsPreferringLatest)(clonedBaseInput);
+        if (options.historyOwnership === 'external') {
+            return {
+                modelInput: { ...base, input: normalizedInput },
+                sourceItems: [],
+                persistedItems: [],
+                filterApplied: false,
+            };
+        }
         return {
             modelInput: { ...base, input: normalizedInput },
             sourceItems: normalizedInput.map((item) => cloneMap.get(item) ?? item),
@@ -47,6 +57,20 @@ async function applyCallModelInputFilter(agent, callModelInputFilter, context, i
         if (!result || !Array.isArray(result.input)) {
             throw new errors_1.UserError('callModelInputFilter must return a ModelInputData object with an input array.');
         }
+        const normalizedInput = (0, items_1.deduplicateAgentInputItemsPreferringLatest)(result.input);
+        if (options.historyOwnership === 'external') {
+            return {
+                modelInput: {
+                    input: normalizedInput,
+                    instructions: typeof result.instructions === 'undefined'
+                        ? systemInstructions
+                        : result.instructions,
+                },
+                sourceItems: [],
+                persistedItems: [],
+                filterApplied: true,
+            };
+        }
         const normalizedBaseSources = (0, items_1.deduplicateAgentInputItemsPreferringLatest)(clonedBaseInput).map((item) => cloneMap.get(item) ?? item);
         const originalPool = (0, items_1.buildAgentInputPool)(normalizedBaseSources);
         const fallbackOriginals = normalizedBaseSources.filter((item) => item && typeof item === 'object');
@@ -66,7 +90,6 @@ async function applyCallModelInputFilter(agent, callModelInputFilter, context, i
             }
             return next;
         };
-        const normalizedInput = (0, items_1.deduplicateAgentInputItemsPreferringLatest)(result.input);
         const consumedExactClones = new WeakSet();
         const injectedExactCloneIndexes = new Set();
         // Preserve a pointer to the original object backing each filtered clone so downstream
diff --git a/dist/runner/conversation.mjs b/dist/runner/conversation.mjs
index 773f1fc48bf8758d642bd51f8c062efbf08f5c45..2cd3fb151bcdfcf8405989ef89e295b939beb951 100644
--- a/dist/runner/conversation.mjs
+++ b/dist/runner/conversation.mjs
@@ -7,7 +7,7 @@ export { getTurnInput } from "./items.mjs";
  * Applies the optional callModelInputFilter and returns the filtered input alongside the original
  * items so downstream tracking and session persistence stay in sync with what the model saw.
  */
-export async function applyCallModelInputFilter(agent, callModelInputFilter, context, inputItems, systemInstructions) {
+export async function applyCallModelInputFilter(agent, callModelInputFilter, context, inputItems, systemInstructions, options = {}) {
     const cloneInputItems = (items, map) => items.map((item) => {
         const cloned = structuredClone(item);
         if (map && cloned && typeof cloned === 'object') {
@@ -17,15 +17,25 @@ export async function applyCallModelInputFilter(agent, callModelInputFilter, con
     });
     // Record the relationship between the cloned array passed to filters and the original inputs.
     const cloneMap = new WeakMap();
-    // Always create a deep copy so downstream mutations inside filters cannot affect
-    // the cached turn state.
-    const clonedBaseInput = cloneInputItems(inputItems, cloneMap);
+    // Application-owned history is a borrowed immutable view: copy only its array
+    // boundary. SDK ownership preserves the defensive deep-clone behavior.
+    const clonedBaseInput = options.historyOwnership === 'external'
+        ? [...inputItems]
+        : cloneInputItems(inputItems, cloneMap);
     const base = {
         input: clonedBaseInput,
         instructions: systemInstructions,
     };
     if (!callModelInputFilter) {
         const normalizedInput = deduplicateAgentInputItemsPreferringLatest(clonedBaseInput);
+        if (options.historyOwnership === 'external') {
+            return {
+                modelInput: { ...base, input: normalizedInput },
+                sourceItems: [],
+                persistedItems: [],
+                filterApplied: false,
+            };
+        }
         return {
             modelInput: { ...base, input: normalizedInput },
             sourceItems: normalizedInput.map((item) => cloneMap.get(item) ?? item),
@@ -42,6 +52,20 @@ export async function applyCallModelInputFilter(agent, callModelInputFilter, con
         if (!result || !Array.isArray(result.input)) {
             throw new UserError('callModelInputFilter must return a ModelInputData object with an input array.');
         }
+        const normalizedInput = deduplicateAgentInputItemsPreferringLatest(result.input);
+        if (options.historyOwnership === 'external') {
+            return {
+                modelInput: {
+                    input: normalizedInput,
+                    instructions: typeof result.instructions === 'undefined'
+                        ? systemInstructions
+                        : result.instructions,
+                },
+                sourceItems: [],
+                persistedItems: [],
+                filterApplied: true,
+            };
+        }
         const normalizedBaseSources = deduplicateAgentInputItemsPreferringLatest(clonedBaseInput).map((item) => cloneMap.get(item) ?? item);
         const originalPool = buildAgentInputPool(normalizedBaseSources);
         const fallbackOriginals = normalizedBaseSources.filter((item) => item && typeof item === 'object');
@@ -61,7 +85,6 @@ export async function applyCallModelInputFilter(agent, callModelInputFilter, con
             }
             return next;
         };
-        const normalizedInput = deduplicateAgentInputItemsPreferringLatest(result.input);
         const consumedExactClones = new WeakSet();
         const injectedExactCloneIndexes = new Set();
         // Preserve a pointer to the original object backing each filtered clone so downstream
diff --git a/dist/runner/modelOutputs.d.ts b/dist/runner/modelOutputs.d.ts
index 3a4f8b679c3c4a9dadf24d0a03fa713ca14d0579..424262b4204e5141e5964895ce220c9bd150db60 100644
--- a/dist/runner/modelOutputs.d.ts
+++ b/dist/runner/modelOutputs.d.ts
@@ -6,9 +6,10 @@ import { RunState } from '../runState';
 import type { AgentInputItem } from '../types';
 import { Tool } from '../tool';
 import type { ProcessedResponse } from './types';
-import type { ToolNotFoundBehavior } from '../run';
+import type { ResolveMissingFunctionTool, ToolNotFoundBehavior } from '../run';
 type ModelResponseProcessingOptions = {
     allowPromptSuppliedTools?: boolean;
+    resolveMissingFunctionTool?: ResolveMissingFunctionTool;
 };
 /**
  * Walks a raw model response and classifies each item so the runner can schedule follow-up work.
diff --git a/dist/runner/modelOutputs.js b/dist/runner/modelOutputs.js
index c9d44147563f79619b754013800494811c180e22..cec577e405ee089f96db19470a153e7acd7defd6 100644
--- a/dist/runner/modelOutputs.js
+++ b/dist/runner/modelOutputs.js
@@ -123,6 +123,66 @@ function resolveFunctionOrHandoff(toolCall, handoffMap, functionMap) {
     }
     return { type: 'function', tool: functionTool };
 }
+function mergeClientToolSearchExecutionTools(availableTools, runtimeTools) {
+    const availableIndexByKey = new Map();
+    for (let index = 0; index < availableTools.length; index++) {
+        const key = (0, tool_1.getToolSearchRuntimeRoutingKey)(availableTools[index]);
+        if (key && !availableIndexByKey.has(key)) {
+            availableIndexByKey.set(key, index);
+        }
+    }
+    for (const runtimeTool of runtimeTools) {
+        const key = (0, tool_1.getToolSearchRuntimeRoutingKey)(runtimeTool);
+        if (!key) {
+            if (!availableTools.includes(runtimeTool)) {
+                availableTools.push(runtimeTool);
+            }
+            continue;
+        }
+        const existingIndex = availableIndexByKey.get(key);
+        if (existingIndex === undefined) {
+            availableIndexByKey.set(key, availableTools.length);
+            availableTools.push(runtimeTool);
+            continue;
+        }
+        if (availableTools[existingIndex] !== runtimeTool) {
+            availableTools[existingIndex] = runtimeTool;
+        }
+    }
+}
+function rememberResolvedMissingFunctionTool(tool, functionMap, availableTools, loadedDeferredToolState) {
+    if (!tool || tool.type !== 'function') {
+        return false;
+    }
+    const runtimeFunctionMap = (0, toolIdentity_1.buildFunctionToolLookupMap)([tool]);
+    for (const [key, mapped] of runtimeFunctionMap) {
+        functionMap.set(key, mapped);
+    }
+    if (Array.isArray(availableTools) && !availableTools.includes(tool)) {
+        availableTools.push(tool);
+    }
+    const qualifiedName = (0, toolIdentity_1.getFunctionToolQualifiedName)(tool);
+    loadedDeferredToolState?.loadedToolNames?.add(tool.name);
+    if (qualifiedName) {
+        loadedDeferredToolState?.loadedToolNames?.add(qualifiedName);
+    }
+    return true;
+}
+async function resolveFunctionOrHandoffMaybeMissing(output, handoffMap, functionMap, agent, processingOptions, availableTools, loadedDeferredToolState) {
+    const resolved = resolveFunctionOrHandoff(output, handoffMap, functionMap);
+    if (resolved.type !== 'not_found' || !processingOptions?.resolveMissingFunctionTool) {
+        return resolved;
+    }
+    const tool = await processingOptions.resolveMissingFunctionTool({
+        name: resolved.toolName,
+        agent,
+        toolCall: output,
+    });
+    if (!rememberResolvedMissingFunctionTool(tool, functionMap, availableTools, loadedDeferredToolState)) {
+        return resolved;
+    }
+    return resolveFunctionOrHandoff(output, handoffMap, functionMap);
+}
 function throwFunctionToolNotFound(toolName, agent) {
     const message = `Tool ${toolName} not found in agent ${agent.name}.`;
     (0, context_1.addErrorToCurrentSpan)({
@@ -307,7 +367,7 @@ async function buildGeneratedClientToolSearchOutputMapAsync(args) {
                 runtimeTools: generatedOutput.runtimeTools,
             });
             generatedOutputs.set(toolSearchCall, generatedOutput);
-            executionTools.push(...generatedOutput.callbackRuntimeTools);
+            mergeClientToolSearchExecutionTools(executionTools, generatedOutput.callbackRuntimeTools);
             continue;
         }
         generatedOutputs.set(toolSearchCall, {
@@ -604,7 +664,8 @@ async function processModelResponseAsync(modelResponse, agent, tools, handoffs,
         ((0, utils_1.getToolSearchExecution)(output) === 'client' ||
             (typeof (0, utils_1.getToolSearchExecution)(output) === 'undefined' &&
                 typeof clientToolSearchTool !== 'undefined')));
-    if (!hasCustomClientToolSearchExecutor || !hasRelevantClientToolSearchCall) {
+    if ((!hasCustomClientToolSearchExecutor || !hasRelevantClientToolSearchCall) &&
+        !processingOptions.resolveMissingFunctionTool) {
         return processModelResponse(modelResponse, agent, tools, handoffs, priorItems, toolNotFoundBehavior, processingOptions);
     }
     const items = [];
@@ -795,7 +856,7 @@ async function processModelResponseAsync(modelResponse, agent, tools, handoffs,
         if (output.type !== 'function_call') {
             continue;
         }
-        const resolved = resolveFunctionOrHandoff(output, handoffMap, functionMap);
+        const resolved = await resolveFunctionOrHandoffMaybeMissing(output, handoffMap, functionMap, agent, processingOptions, availableTools, loadedDeferredToolState);
         if (resolved.type === 'not_found') {
             if (toolNotFoundBehavior !== 'return_error_to_model') {
                 throwFunctionToolNotFound(resolved.toolName, agent);
diff --git a/dist/runner/modelOutputs.mjs b/dist/runner/modelOutputs.mjs
index 34dc1b761aee009ea97f2a26529f7246c44d018e..4ff195c2cfe634b56b942a675491b1f24482f069 100644
--- a/dist/runner/modelOutputs.mjs
+++ b/dist/runner/modelOutputs.mjs
@@ -119,6 +119,66 @@ function resolveFunctionOrHandoff(toolCall, handoffMap, functionMap) {
     }
     return { type: 'function', tool: functionTool };
 }
+function mergeClientToolSearchExecutionTools(availableTools, runtimeTools) {
+    const availableIndexByKey = new Map();
+    for (let index = 0; index < availableTools.length; index++) {
+        const key = getToolSearchRuntimeRoutingKey(availableTools[index]);
+        if (key && !availableIndexByKey.has(key)) {
+            availableIndexByKey.set(key, index);
+        }
+    }
+    for (const runtimeTool of runtimeTools) {
+        const key = getToolSearchRuntimeRoutingKey(runtimeTool);
+        if (!key) {
+            if (!availableTools.includes(runtimeTool)) {
+                availableTools.push(runtimeTool);
+            }
+            continue;
+        }
+        const existingIndex = availableIndexByKey.get(key);
+        if (existingIndex === undefined) {
+            availableIndexByKey.set(key, availableTools.length);
+            availableTools.push(runtimeTool);
+            continue;
+        }
+        if (availableTools[existingIndex] !== runtimeTool) {
+            availableTools[existingIndex] = runtimeTool;
+        }
+    }
+}
+function rememberResolvedMissingFunctionTool(tool, functionMap, availableTools, loadedDeferredToolState) {
+    if (!tool || tool.type !== 'function') {
+        return false;
+    }
+    const runtimeFunctionMap = buildFunctionToolLookupMap([tool]);
+    for (const [key, mapped] of runtimeFunctionMap) {
+        functionMap.set(key, mapped);
+    }
+    if (Array.isArray(availableTools) && !availableTools.includes(tool)) {
+        availableTools.push(tool);
+    }
+    const qualifiedName = getFunctionToolQualifiedName(tool);
+    loadedDeferredToolState?.loadedToolNames?.add(tool.name);
+    if (qualifiedName) {
+        loadedDeferredToolState?.loadedToolNames?.add(qualifiedName);
+    }
+    return true;
+}
+async function resolveFunctionOrHandoffMaybeMissing(output, handoffMap, functionMap, agent, processingOptions, availableTools, loadedDeferredToolState) {
+    const resolved = resolveFunctionOrHandoff(output, handoffMap, functionMap);
+    if (resolved.type !== 'not_found' || !processingOptions?.resolveMissingFunctionTool) {
+        return resolved;
+    }
+    const tool = await processingOptions.resolveMissingFunctionTool({
+        name: resolved.toolName,
+        agent,
+        toolCall: output,
+    });
+    if (!rememberResolvedMissingFunctionTool(tool, functionMap, availableTools, loadedDeferredToolState)) {
+        return resolved;
+    }
+    return resolveFunctionOrHandoff(output, handoffMap, functionMap);
+}
 function throwFunctionToolNotFound(toolName, agent) {
     const message = `Tool ${toolName} not found in agent ${agent.name}.`;
     addErrorToCurrentSpan({
@@ -303,7 +363,7 @@ async function buildGeneratedClientToolSearchOutputMapAsync(args) {
                 runtimeTools: generatedOutput.runtimeTools,
             });
             generatedOutputs.set(toolSearchCall, generatedOutput);
-            executionTools.push(...generatedOutput.callbackRuntimeTools);
+            mergeClientToolSearchExecutionTools(executionTools, generatedOutput.callbackRuntimeTools);
             continue;
         }
         generatedOutputs.set(toolSearchCall, {
@@ -600,7 +660,8 @@ export async function processModelResponseAsync(modelResponse, agent, tools, han
         (getToolSearchExecution(output) === 'client' ||
             (typeof getToolSearchExecution(output) === 'undefined' &&
                 typeof clientToolSearchTool !== 'undefined')));
-    if (!hasCustomClientToolSearchExecutor || !hasRelevantClientToolSearchCall) {
+    if ((!hasCustomClientToolSearchExecutor || !hasRelevantClientToolSearchCall) &&
+        !processingOptions.resolveMissingFunctionTool) {
         return processModelResponse(modelResponse, agent, tools, handoffs, priorItems, toolNotFoundBehavior, processingOptions);
     }
     const items = [];
@@ -791,7 +852,7 @@ export async function processModelResponseAsync(modelResponse, agent, tools, han
         if (output.type !== 'function_call') {
             continue;
         }
-        const resolved = resolveFunctionOrHandoff(output, handoffMap, functionMap);
+        const resolved = await resolveFunctionOrHandoffMaybeMissing(output, handoffMap, functionMap, agent, processingOptions, availableTools, loadedDeferredToolState);
         if (resolved.type === 'not_found') {
             if (toolNotFoundBehavior !== 'return_error_to_model') {
                 throwFunctionToolNotFound(resolved.toolName, agent);
diff --git a/dist/sandbox/manifest.js b/dist/sandbox/manifest.js
index c7f81e3d0e9bdc08c475371c1cd6ccd508722a69..ba577c045f4fc776fe72382b4cae41f658e3c096 100644
--- a/dist/sandbox/manifest.js
+++ b/dist/sandbox/manifest.js
@@ -415,14 +415,30 @@ function normalizeRoot(root) {
     if ((0, posixPath_1.hasBackslashPathSeparator)(root)) {
         throw new Error(`Manifest root "${root}" must use "/" separators.`);
     }
-    if (!trimmed.startsWith('/')) {
+    if (!trimmed.startsWith('/') && !isWindowsDriveAbsolutePath(trimmed)) {
         throw new Error(`Manifest root "${root}" must be absolute.`);
     }
     if ((0, posixPath_1.hasParentPathSegment)(root)) {
         throw new Error(`Manifest root "${root}" must not contain parent segments.`);
     }
+    if (isWindowsDriveAbsolutePath(trimmed)) {
+        const normalized = (0, posixPath_1.normalizePosixPath)(trimmed.slice(3));
+        return normalized === '.' ? trimmed.slice(0, 3) : `${trimmed.slice(0, 3)}${normalized}`;
+    }
+    if (isWindowsUncAbsolutePath(trimmed)) {
+        const segments = trimmed.slice(2).split('/').filter(Boolean);
+        const prefix = `//${segments[0]}/${segments[1]}`;
+        const normalized = (0, posixPath_1.normalizePosixPath)(segments.slice(2).join('/'));
+        return normalized === '.' ? prefix : `${prefix}/${normalized}`;
+    }
     return (0, posixPath_1.normalizePosixPath)(root);
 }
+function isWindowsDriveAbsolutePath(path) {
+    return /^[A-Za-z]:\//u.test(path);
+}
+function isWindowsUncAbsolutePath(path) {
+    return /^\/\/[^/]+\/[^/]+(?:\/|$)/u.test(path);
+}
 function joinAbsolutePath(root, relativePath) {
     const normalizedRoot = normalizeRoot(root);
     const normalizedRelativePath = normalizeRelativePath(relativePath);
diff --git a/dist/sandbox/manifest.mjs b/dist/sandbox/manifest.mjs
index 37b655b60d16747b5b2aa72b0f827e7b05604a92..4938c08c6239dee1ac6714fe919fddc54c8133c4 100644
--- a/dist/sandbox/manifest.mjs
+++ b/dist/sandbox/manifest.mjs
@@ -402,14 +402,30 @@ function normalizeRoot(root) {
     if (hasBackslashPathSeparator(root)) {
         throw new Error(`Manifest root "${root}" must use "/" separators.`);
     }
-    if (!trimmed.startsWith('/')) {
+    if (!trimmed.startsWith('/') && !isWindowsDriveAbsolutePath(trimmed)) {
         throw new Error(`Manifest root "${root}" must be absolute.`);
     }
     if (hasParentPathSegment(root)) {
         throw new Error(`Manifest root "${root}" must not contain parent segments.`);
     }
+    if (isWindowsDriveAbsolutePath(trimmed)) {
+        const normalized = normalizePosixPath(trimmed.slice(3));
+        return normalized === '.' ? trimmed.slice(0, 3) : `${trimmed.slice(0, 3)}${normalized}`;
+    }
+    if (isWindowsUncAbsolutePath(trimmed)) {
+        const segments = trimmed.slice(2).split('/').filter(Boolean);
+        const prefix = `//${segments[0]}/${segments[1]}`;
+        const normalized = normalizePosixPath(segments.slice(2).join('/'));
+        return normalized === '.' ? prefix : `${prefix}/${normalized}`;
+    }
     return normalizePosixPath(root);
 }
+function isWindowsDriveAbsolutePath(path) {
+    return /^[A-Za-z]:\//u.test(path);
+}
+function isWindowsUncAbsolutePath(path) {
+    return /^\/\/[^/]+\/[^/]+(?:\/|$)/u.test(path);
+}
 function joinAbsolutePath(root, relativePath) {
     const normalizedRoot = normalizeRoot(root);
     const normalizedRelativePath = normalizeRelativePath(relativePath);
diff --git a/dist/sandbox/workspacePaths.js b/dist/sandbox/workspacePaths.js
index 30fb71f0d5b40c0361d1bbbf0caa80bf3b3e2a34..711435ff36469ea29f71ccae93ab309b2989c680 100644
--- a/dist/sandbox/workspacePaths.js
+++ b/dist/sandbox/workspacePaths.js
@@ -7,19 +7,23 @@ const pathGrants_1 = require("./pathGrants.js");
 const posixPath_1 = require("./shared/posixPath.js");
 class WorkspacePathPolicy {
     root;
+    windows;
     extraPathGrants;
     constructor(options) {
         if ((0, posixPath_1.hasParentPathSegment)(options.root)) {
             throw new errors_1.UserError('Sandbox workspace root must not contain parent segments.');
         }
-        if ((0, posixPath_1.hasBackslashPathSeparator)(options.root)) {
+        const portableRoot = options.root.replaceAll('\\', '/');
+        const windows = isWindowsAbsolutePath(portableRoot);
+        if (!windows && (0, posixPath_1.hasBackslashPathSeparator)(options.root)) {
             throw new errors_1.UserError(`Sandbox path "${options.root}" must use "/" separators.`);
         }
-        const root = (0, posixPath_1.normalizePosixPath)(options.root);
-        if (!root.startsWith('/')) {
+        const root = normalizePortablePath(portableRoot, windows);
+        if (!root.startsWith('/') && !isWindowsDriveAbsolutePath(root)) {
             throw new errors_1.UserError('Sandbox workspace root must be absolute.');
         }
         this.root = root;
+        this.windows = windows;
         this.extraPathGrants = (options.extraPathGrants ?? [])
             .map((grant) => (0, pathGrants_1.normalizePathGrant)(grant))
             .sort((left, right) => right.path.length - left.path.length);
@@ -27,12 +31,13 @@ class WorkspacePathPolicy {
     resolve(path, options = {}) {
         const originalPath = path ?? '';
         const trimmed = originalPath.trim();
+        const portablePath = this.windows ? trimmed.replaceAll('\\', '/') : trimmed;
         const normalizedPath = trimmed.length === 0
             ? this.root
-            : trimmed.startsWith('/')
-                ? normalizeSandboxPath(trimmed, originalPath)
-                : joinPosixPath(this.root, normalizeSandboxPath(trimmed, originalPath));
-        const workspaceRelativePath = (0, posixPath_1.relativePosixPathWithinRoot)(this.root, normalizedPath);
+            : isPortableAbsolutePath(portablePath)
+                ? normalizeSandboxPath(portablePath, originalPath, this.windows)
+                : joinPortablePath(this.root, normalizeSandboxPath(portablePath, originalPath, this.windows), this.windows);
+        const workspaceRelativePath = relativePortablePathWithinRoot(this.root, normalizedPath, this.windows);
         if (workspaceRelativePath !== null) {
             return {
                 path: normalizedPath,
@@ -56,19 +61,53 @@ class WorkspacePathPolicy {
     }
 }
 exports.WorkspacePathPolicy = WorkspacePathPolicy;
-function joinPosixPath(root, relativePath) {
+function joinPortablePath(root, relativePath, windows) {
     if (!relativePath) {
-        return (0, posixPath_1.normalizePosixPath)(root);
+        return normalizePortablePath(root, windows);
     }
-    return (0, posixPath_1.normalizePosixPath)(`${root.replace(/\/+$/u, '')}/${relativePath}`);
+    return normalizePortablePath(`${root.replace(/\/+$/u, '')}/${relativePath}`, windows);
 }
-function normalizeSandboxPath(path, originalPath) {
-    if ((0, posixPath_1.hasBackslashPathSeparator)(path)) {
+function normalizeSandboxPath(path, originalPath, windows) {
+    if (!windows && (0, posixPath_1.hasBackslashPathSeparator)(path)) {
         throw new errors_1.UserError(`Sandbox path "${originalPath}" must use "/" separators.`);
     }
     if ((0, posixPath_1.hasEscapingParentPathSegment)(path)) {
         throw new errors_2.SandboxInvalidManifestPathError(`Sandbox path "${originalPath}" must not escape root.`);
     }
+    return normalizePortablePath(path, windows);
+}
+function isWindowsDriveAbsolutePath(path) {
+    return /^[A-Za-z]:\//u.test(path);
+}
+function isWindowsUncAbsolutePath(path) {
+    return /^\/\/[^/]+\/[^/]+(?:\/|$)/u.test(path);
+}
+function isWindowsAbsolutePath(path) {
+    return isWindowsDriveAbsolutePath(path) || isWindowsUncAbsolutePath(path);
+}
+function isPortableAbsolutePath(path) {
+    return path.startsWith('/') || isWindowsAbsolutePath(path);
+}
+function normalizePortablePath(path, windows) {
+    if (!windows) return (0, posixPath_1.normalizePosixPath)(path);
+    if (isWindowsDriveAbsolutePath(path)) {
+        const normalized = (0, posixPath_1.normalizePosixPath)(path.slice(3));
+        return normalized === '.' ? path.slice(0, 3) : `${path.slice(0, 3)}${normalized}`;
+    }
+    if (isWindowsUncAbsolutePath(path)) {
+        const segments = path.slice(2).split('/').filter(Boolean);
+        const prefix = `//${segments[0]}/${segments[1]}`;
+        const normalized = (0, posixPath_1.normalizePosixPath)(segments.slice(2).join('/'));
+        return normalized === '.' ? prefix : `${prefix}/${normalized}`;
+    }
     return (0, posixPath_1.normalizePosixPath)(path);
 }
+function relativePortablePathWithinRoot(root, path, windows) {
+    if (!windows) return (0, posixPath_1.relativePosixPathWithinRoot)(root, path);
+    const rootKey = root.toLowerCase();
+    const pathKey = path.toLowerCase();
+    if (pathKey === rootKey) return '';
+    const prefix = rootKey.endsWith('/') ? rootKey : `${rootKey}/`;
+    return pathKey.startsWith(prefix) ? path.slice(prefix.length) : null;
+}
 //# sourceMappingURL=workspacePaths.js.map
\ No newline at end of file
diff --git a/dist/sandbox/workspacePaths.mjs b/dist/sandbox/workspacePaths.mjs
index fc2aa3141933c96cc080d65b05f7cffb13ee18d7..c4324d271c7bc90128dad0285198f68703c330ef 100644
--- a/dist/sandbox/workspacePaths.mjs
+++ b/dist/sandbox/workspacePaths.mjs
@@ -4,19 +4,23 @@ import { normalizePathGrant, } from "./pathGrants.mjs";
 import { hasBackslashPathSeparator, hasEscapingParentPathSegment, hasParentPathSegment, isUnderPosixPath, normalizePosixPath, relativePosixPathWithinRoot, } from "./shared/posixPath.mjs";
 export class WorkspacePathPolicy {
     root;
+    windows;
     extraPathGrants;
     constructor(options) {
         if (hasParentPathSegment(options.root)) {
             throw new UserError('Sandbox workspace root must not contain parent segments.');
         }
-        if (hasBackslashPathSeparator(options.root)) {
+        const portableRoot = options.root.replaceAll('\\', '/');
+        const windows = isWindowsAbsolutePath(portableRoot);
+        if (!windows && hasBackslashPathSeparator(options.root)) {
             throw new UserError(`Sandbox path "${options.root}" must use "/" separators.`);
         }
-        const root = normalizePosixPath(options.root);
-        if (!root.startsWith('/')) {
+        const root = normalizePortablePath(portableRoot, windows);
+        if (!root.startsWith('/') && !isWindowsDriveAbsolutePath(root)) {
             throw new UserError('Sandbox workspace root must be absolute.');
         }
         this.root = root;
+        this.windows = windows;
         this.extraPathGrants = (options.extraPathGrants ?? [])
             .map((grant) => normalizePathGrant(grant))
             .sort((left, right) => right.path.length - left.path.length);
@@ -24,12 +28,13 @@ export class WorkspacePathPolicy {
     resolve(path, options = {}) {
         const originalPath = path ?? '';
         const trimmed = originalPath.trim();
+        const portablePath = this.windows ? trimmed.replaceAll('\\', '/') : trimmed;
         const normalizedPath = trimmed.length === 0
             ? this.root
-            : trimmed.startsWith('/')
-                ? normalizeSandboxPath(trimmed, originalPath)
-                : joinPosixPath(this.root, normalizeSandboxPath(trimmed, originalPath));
-        const workspaceRelativePath = relativePosixPathWithinRoot(this.root, normalizedPath);
+            : isPortableAbsolutePath(portablePath)
+                ? normalizeSandboxPath(portablePath, originalPath, this.windows)
+                : joinPortablePath(this.root, normalizeSandboxPath(portablePath, originalPath, this.windows), this.windows);
+        const workspaceRelativePath = relativePortablePathWithinRoot(this.root, normalizedPath, this.windows);
         if (workspaceRelativePath !== null) {
             return {
                 path: normalizedPath,
@@ -52,19 +57,53 @@ export class WorkspacePathPolicy {
         return this.extraPathGrants.find((grant) => isUnderPosixPath(path, grant.path));
     }
 }
-function joinPosixPath(root, relativePath) {
+function joinPortablePath(root, relativePath, windows) {
     if (!relativePath) {
-        return normalizePosixPath(root);
+        return normalizePortablePath(root, windows);
     }
-    return normalizePosixPath(`${root.replace(/\/+$/u, '')}/${relativePath}`);
+    return normalizePortablePath(`${root.replace(/\/+$/u, '')}/${relativePath}`, windows);
 }
-function normalizeSandboxPath(path, originalPath) {
-    if (hasBackslashPathSeparator(path)) {
+function normalizeSandboxPath(path, originalPath, windows) {
+    if (!windows && hasBackslashPathSeparator(path)) {
         throw new UserError(`Sandbox path "${originalPath}" must use "/" separators.`);
     }
     if (hasEscapingParentPathSegment(path)) {
         throw new SandboxInvalidManifestPathError(`Sandbox path "${originalPath}" must not escape root.`);
     }
+    return normalizePortablePath(path, windows);
+}
+function isWindowsDriveAbsolutePath(path) {
+    return /^[A-Za-z]:\//u.test(path);
+}
+function isWindowsUncAbsolutePath(path) {
+    return /^\/\/[^/]+\/[^/]+(?:\/|$)/u.test(path);
+}
+function isWindowsAbsolutePath(path) {
+    return isWindowsDriveAbsolutePath(path) || isWindowsUncAbsolutePath(path);
+}
+function isPortableAbsolutePath(path) {
+    return path.startsWith('/') || isWindowsAbsolutePath(path);
+}
+function normalizePortablePath(path, windows) {
+    if (!windows) return normalizePosixPath(path);
+    if (isWindowsDriveAbsolutePath(path)) {
+        const normalized = normalizePosixPath(path.slice(3));
+        return normalized === '.' ? path.slice(0, 3) : `${path.slice(0, 3)}${normalized}`;
+    }
+    if (isWindowsUncAbsolutePath(path)) {
+        const segments = path.slice(2).split('/').filter(Boolean);
+        const prefix = `//${segments[0]}/${segments[1]}`;
+        const normalized = normalizePosixPath(segments.slice(2).join('/'));
+        return normalized === '.' ? prefix : `${prefix}/${normalized}`;
+    }
     return normalizePosixPath(path);
 }
+function relativePortablePathWithinRoot(root, path, windows) {
+    if (!windows) return relativePosixPathWithinRoot(root, path);
+    const rootKey = root.toLowerCase();
+    const pathKey = path.toLowerCase();
+    if (pathKey === rootKey) return '';
+    const prefix = rootKey.endsWith('/') ? rootKey : `${rootKey}/`;
+    return pathKey.startsWith(prefix) ? path.slice(prefix.length) : null;
+}
 //# sourceMappingURL=workspacePaths.mjs.map
\ No newline at end of file
