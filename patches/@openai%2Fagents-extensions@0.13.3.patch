diff --git a/dist/sandbox/blaxel/sandbox.d.ts b/dist/sandbox/blaxel/sandbox.d.ts
index e2dccf6b67a01d9c7f9f6d12d9716ed3a36241f8..a5027031e230f862c34a1a284e86ab34bb050e09 100644
--- a/dist/sandbox/blaxel/sandbox.d.ts
+++ b/dist/sandbox/blaxel/sandbox.d.ts
@@ -152,7 +152,10 @@ export declare class BlaxelSandboxClient implements SandboxClient<BlaxelSandboxC
     serializeSessionState(state: BlaxelSandboxSessionState): Promise<Record<string, unknown>>;
     canPersistOwnedSessionState(state: BlaxelSandboxSessionState): boolean;
     deserializeSessionState(state: Record<string, unknown>): Promise<BlaxelSandboxSessionState>;
-    resume(state: BlaxelSandboxSessionState): Promise<BlaxelSandboxSession>;
+    resume(state: BlaxelSandboxSessionState, options?: {
+        recreateIfMissing?: boolean;
+    }): Promise<BlaxelSandboxSession>;
+    resumeExact(state: BlaxelSandboxSessionState): Promise<BlaxelSandboxSession>;
     private recreateFromState;
 }
 export {};
diff --git a/dist/sandbox/blaxel/sandbox.js b/dist/sandbox/blaxel/sandbox.js
index 75a9f5cffd20a50213b914c95a9c08ab0d8e03a1..f1d371c51c2f9658fd4c77726ef5054cc3afb2d9 100644
--- a/dist/sandbox/blaxel/sandbox.js
+++ b/dist/sandbox/blaxel/sandbox.js
@@ -643,7 +643,7 @@ class BlaxelSandboxClient {
             ownsSandbox: (0, shared_1.readOptionalBoolean)(state, 'ownsSandbox') ?? true,
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const SandboxInstance = await loadBlaxelSandboxClass();
         let sandbox;
         try {
@@ -655,9 +655,21 @@ class BlaxelSandboxClient {
                 provider: 'blaxel',
                 details: { sandboxName: state.sandboxName },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             return await this.recreateFromState(state);
         }
         if (isBlaxelSandboxNonResumable(sandbox)) {
+            if (options.recreateIfMissing === false) {
+                const unavailable = new sandbox_1.SandboxProviderError(`Blaxel sandbox ${state.sandboxName} is no longer resumable because its status is ${sandbox.status ?? 'unknown'}.`, {
+                    provider: 'blaxel',
+                    sandboxName: state.sandboxName,
+                    status: sandbox.status,
+                });
+                unavailable.code = 'SANDBOX_NOT_FOUND';
+                throw unavailable;
+            }
             return await this.recreateFromState(state);
         }
         if (state.ownsSandbox) {
@@ -677,6 +689,9 @@ class BlaxelSandboxClient {
         await session.rehydrateActiveMountPathsFromManifest();
         return session;
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
     async recreateFromState(state) {
         const session = await this.create(state.manifest, {
             image: state.image,
diff --git a/dist/sandbox/blaxel/sandbox.mjs b/dist/sandbox/blaxel/sandbox.mjs
index 8cfe8a9fa5ece567809b29eb9e662d6f570eded0..d3bc7cf1571182f615a21e7d356d8d45972625b3 100644
--- a/dist/sandbox/blaxel/sandbox.mjs
+++ b/dist/sandbox/blaxel/sandbox.mjs
@@ -606,7 +606,7 @@ export class BlaxelSandboxClient {
             ownsSandbox: readOptionalBoolean(state, 'ownsSandbox') ?? true,
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const SandboxInstance = await loadBlaxelSandboxClass();
         let sandbox;
         try {
@@ -618,9 +618,21 @@ export class BlaxelSandboxClient {
                 provider: 'blaxel',
                 details: { sandboxName: state.sandboxName },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             return await this.recreateFromState(state);
         }
         if (isBlaxelSandboxNonResumable(sandbox)) {
+            if (options.recreateIfMissing === false) {
+                const unavailable = new SandboxProviderError(`Blaxel sandbox ${state.sandboxName} is no longer resumable because its status is ${sandbox.status ?? 'unknown'}.`, {
+                    provider: 'blaxel',
+                    sandboxName: state.sandboxName,
+                    status: sandbox.status,
+                });
+                unavailable.code = 'SANDBOX_NOT_FOUND';
+                throw unavailable;
+            }
             return await this.recreateFromState(state);
         }
         if (state.ownsSandbox) {
@@ -640,6 +652,9 @@ export class BlaxelSandboxClient {
         await session.rehydrateActiveMountPathsFromManifest();
         return session;
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
     async recreateFromState(state) {
         const session = await this.create(state.manifest, {
             image: state.image,
diff --git a/dist/sandbox/daytona/sandbox.d.ts b/dist/sandbox/daytona/sandbox.d.ts
index 8374847c4a8064b1f17ae45771732c5f13cceb5d..393a532dc98e80c91c621fb3e3206f39247ab0de 100644
--- a/dist/sandbox/daytona/sandbox.d.ts
+++ b/dist/sandbox/daytona/sandbox.d.ts
@@ -158,7 +158,10 @@ export declare class DaytonaSandboxClient implements SandboxClient<DaytonaSandbo
     serializeSessionState(state: DaytonaSandboxSessionState): Promise<Record<string, unknown>>;
     canPersistOwnedSessionState(state: DaytonaSandboxSessionState): boolean;
     deserializeSessionState(state: Record<string, unknown>): Promise<DaytonaSandboxSessionState>;
-    resume(state: DaytonaSandboxSessionState): Promise<DaytonaSandboxSession>;
+    resume(state: DaytonaSandboxSessionState, options?: {
+        recreateIfMissing?: boolean;
+    }): Promise<DaytonaSandboxSession>;
+    resumeExact(state: DaytonaSandboxSessionState): Promise<DaytonaSandboxSession>;
     private recreateFromPersistedState;
 }
 export {};
diff --git a/dist/sandbox/daytona/sandbox.js b/dist/sandbox/daytona/sandbox.js
index ebb08cc7e1c58d71db672793dda2842a340e837d..99c15e0383ff9b9f27a951c47fe778c1186dee72 100644
--- a/dist/sandbox/daytona/sandbox.js
+++ b/dist/sandbox/daytona/sandbox.js
@@ -738,7 +738,7 @@ class DaytonaSandboxClient {
             target: (0, shared_1.readOptionalString)(state, 'target'),
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const client = await createDaytonaClient({
             ...this.options,
             apiKey: state.apiKey ?? this.options.apiKey,
@@ -756,6 +756,9 @@ class DaytonaSandboxClient {
                 provider: 'daytona',
                 details: { sandboxId: state.sandboxId },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             return await this.recreateFromPersistedState(client, state);
         }
         const session = new DaytonaSandboxSession({
@@ -767,6 +770,9 @@ class DaytonaSandboxClient {
         await session.rematerializeMountEntries();
         return session;
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
     async recreateFromPersistedState(client, state) {
         const sandbox = await (0, shared_1.withProviderError)('DaytonaSandboxClient', 'daytona', 'create sandbox', async () => await client.create({
             ...(state.sandboxSnapshotName
diff --git a/dist/sandbox/daytona/sandbox.mjs b/dist/sandbox/daytona/sandbox.mjs
index f3e700556ad3560379ed16a67fddf5fd24760817..2def2268c78a46446577bbcdb34d513148a52d46 100644
--- a/dist/sandbox/daytona/sandbox.mjs
+++ b/dist/sandbox/daytona/sandbox.mjs
@@ -701,7 +701,7 @@ export class DaytonaSandboxClient {
             target: readOptionalString(state, 'target'),
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const client = await createDaytonaClient({
             ...this.options,
             apiKey: state.apiKey ?? this.options.apiKey,
@@ -719,6 +719,9 @@ export class DaytonaSandboxClient {
                 provider: 'daytona',
                 details: { sandboxId: state.sandboxId },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             return await this.recreateFromPersistedState(client, state);
         }
         const session = new DaytonaSandboxSession({
@@ -730,6 +733,9 @@ export class DaytonaSandboxClient {
         await session.rematerializeMountEntries();
         return session;
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
     async recreateFromPersistedState(client, state) {
         const sandbox = await withProviderError('DaytonaSandboxClient', 'daytona', 'create sandbox', async () => await client.create({
             ...(state.sandboxSnapshotName
diff --git a/dist/sandbox/e2b/sandbox.d.ts b/dist/sandbox/e2b/sandbox.d.ts
index 397c9f7809a4e78843db14e8a16385f659a6cf1f..ba9b2f5c881f564a29d83eaa2966aa2f70553e94 100644
--- a/dist/sandbox/e2b/sandbox.d.ts
+++ b/dist/sandbox/e2b/sandbox.d.ts
@@ -166,6 +166,9 @@ export declare class E2BSandboxClient implements SandboxClient<E2BSandboxClientO
     serializeSessionState(state: E2BSandboxSessionState): Promise<Record<string, unknown>>;
     canPersistOwnedSessionState(state: E2BSandboxSessionState): boolean;
     deserializeSessionState(state: Record<string, unknown>): Promise<E2BSandboxSessionState>;
-    resume(state: E2BSandboxSessionState): Promise<E2BSandboxSession>;
+    resume(state: E2BSandboxSessionState, options?: {
+        recreateIfMissing?: boolean;
+    }): Promise<E2BSandboxSession>;
+    resumeExact(state: E2BSandboxSessionState): Promise<E2BSandboxSession>;
 }
 export {};
diff --git a/dist/sandbox/e2b/sandbox.js b/dist/sandbox/e2b/sandbox.js
index ccf3b1262ac248ff2f3eb178a18759ede5c53ef6..8e69ce267147788c8212735a3d24a0f74cd00405 100644
--- a/dist/sandbox/e2b/sandbox.js
+++ b/dist/sandbox/e2b/sandbox.js
@@ -616,7 +616,7 @@ class E2BSandboxClient {
             pauseOnExitSupported: (0, shared_1.readOptionalBoolean)(state, 'pauseOnExitSupported') ?? false,
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const Sandbox = await loadE2BSandboxClass(state.sandboxType);
         const connect = Sandbox.connect ?? Sandbox.resume;
         if (connect) {
@@ -634,14 +634,23 @@ class E2BSandboxClient {
                     provider: 'e2b',
                     details: { sandboxId: state.sandboxId },
                 });
+                if (options.recreateIfMissing === false) {
+                    throw error;
+                }
                 // Fall through to recreate from serialized state.
             }
         }
+        else if (options.recreateIfMissing === false) {
+            throw new agents_core_1.UserError('E2BSandboxClient cannot exactly resume because the installed E2B SDK exposes no connect or resume operation.');
+        }
         return await this.create(state.manifest, {
             ...stateToCreateOptions(state),
             env: state.environment,
         });
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
 }
 exports.E2BSandboxClient = E2BSandboxClient;
 function validateOptions(options) {
diff --git a/dist/sandbox/e2b/sandbox.mjs b/dist/sandbox/e2b/sandbox.mjs
index 769abca8cf0bd3b528eab42e991c8e1494e47f3d..349c5f10c4b752feb5776f74dda07589e28a7d89 100644
--- a/dist/sandbox/e2b/sandbox.mjs
+++ b/dist/sandbox/e2b/sandbox.mjs
@@ -579,7 +579,7 @@ export class E2BSandboxClient {
             pauseOnExitSupported: readOptionalBoolean(state, 'pauseOnExitSupported') ?? false,
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const Sandbox = await loadE2BSandboxClass(state.sandboxType);
         const connect = Sandbox.connect ?? Sandbox.resume;
         if (connect) {
@@ -597,14 +597,23 @@ export class E2BSandboxClient {
                     provider: 'e2b',
                     details: { sandboxId: state.sandboxId },
                 });
+                if (options.recreateIfMissing === false) {
+                    throw error;
+                }
                 // Fall through to recreate from serialized state.
             }
         }
+        else if (options.recreateIfMissing === false) {
+            throw new UserError('E2BSandboxClient cannot exactly resume because the installed E2B SDK exposes no connect or resume operation.');
+        }
         return await this.create(state.manifest, {
             ...stateToCreateOptions(state),
             env: state.environment,
         });
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
 }
 function validateOptions(options) {
     readE2BWorkspacePersistence(options.workspacePersistence);
diff --git a/dist/sandbox/runloop/sandbox.d.ts b/dist/sandbox/runloop/sandbox.d.ts
index 99e9131d49c18987c997adf493795a648cd8f261..7484dd5d704ca3b22b095eaffdf43b2823258eb1 100644
--- a/dist/sandbox/runloop/sandbox.d.ts
+++ b/dist/sandbox/runloop/sandbox.d.ts
@@ -263,6 +263,9 @@ export declare class RunloopSandboxClient implements SandboxClient<RunloopSandbo
     serializeSessionState(state: RunloopSandboxSessionState): Promise<Record<string, unknown>>;
     canPersistOwnedSessionState(state: RunloopSandboxSessionState): boolean;
     deserializeSessionState(state: Record<string, unknown>): Promise<RunloopSandboxSessionState>;
-    resume(state: RunloopSandboxSessionState): Promise<RunloopSandboxSession>;
+    resume(state: RunloopSandboxSessionState, options?: {
+        recreateIfMissing?: boolean;
+    }): Promise<RunloopSandboxSession>;
+    resumeExact(state: RunloopSandboxSessionState): Promise<RunloopSandboxSession>;
 }
 export {};
diff --git a/dist/sandbox/runloop/sandbox.js b/dist/sandbox/runloop/sandbox.js
index 0918e99b5306e1b5fecd37aff4416d3617c9073a..d4ca98e457bccad4b12cbe3245aa90aee62b586a 100644
--- a/dist/sandbox/runloop/sandbox.js
+++ b/dist/sandbox/runloop/sandbox.js
@@ -844,7 +844,7 @@ class RunloopSandboxClient {
             }),
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const resumeState = {
             ...state,
             baseUrl: this.options.baseUrl,
@@ -881,6 +881,9 @@ class RunloopSandboxClient {
                 provider: 'runloop',
                 details: { devboxId: resumeState.devboxId },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             assertRunloopResumeRecreateSecretRefsTrusted(resumeState, this.options);
             const recreateOptions = {
                 blueprintName: this.options.blueprintName,
@@ -903,6 +906,9 @@ class RunloopSandboxClient {
             return await this.create(resumeState.manifest, recreateOptions);
         }
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
 }
 exports.RunloopSandboxClient = RunloopSandboxClient;
 async function createRunloopClient(options) {
diff --git a/dist/sandbox/runloop/sandbox.mjs b/dist/sandbox/runloop/sandbox.mjs
index d5caab92aa6b78c130aaa3912e6ee818ee79982e..a180a8ccb040cf41426ffa31bca68a593e1709ab 100644
--- a/dist/sandbox/runloop/sandbox.mjs
+++ b/dist/sandbox/runloop/sandbox.mjs
@@ -801,7 +801,7 @@ export class RunloopSandboxClient {
             }),
         };
     }
-    async resume(state) {
+    async resume(state, options = {}) {
         const resumeState = {
             ...state,
             baseUrl: this.options.baseUrl,
@@ -838,6 +838,9 @@ export class RunloopSandboxClient {
                 provider: 'runloop',
                 details: { devboxId: resumeState.devboxId },
             });
+            if (options.recreateIfMissing === false) {
+                throw error;
+            }
             assertRunloopResumeRecreateSecretRefsTrusted(resumeState, this.options);
             const recreateOptions = {
                 blueprintName: this.options.blueprintName,
@@ -860,6 +863,9 @@ export class RunloopSandboxClient {
             return await this.create(resumeState.manifest, recreateOptions);
         }
     }
+    async resumeExact(state) {
+        return await this.resume(state, { recreateIfMissing: false });
+    }
 }
 async function createRunloopClient(options) {
     try {
