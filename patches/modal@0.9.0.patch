diff --git a/dist/index.cjs b/dist/index.cjs
index e1794ee4e42d61ebb0c35654318fe5c84a946b04..69851068b54df299ddf00a5db8981d38611cc5ba 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -56987,9 +56987,9 @@ var ImageService = class {
    *
    * @param imageId - Image ID.
    */
-  async fromId(imageId) {
+  async fromId(imageId, params) {
     try {
-      const resp = await this.#client.cpClient.imageFromId({ imageId });
+      const resp = await this.#client.cpClient.imageFromId({ imageId }, params);
       return new Image2(this.#client, resp.imageId, "");
     } catch (err) {
       if (err instanceof import_nice_grpc7.ClientError && err.code === import_nice_grpc8.Status.NOT_FOUND)
@@ -61900,6 +61900,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
   jwtExp;
   jwtRefreshLock = Promise.resolve();
   logger;
+  profile;
   closed = false;
   static async tryInit(serverClient, taskId, sandboxId, isV2, logger2, profile) {
     let resp;
@@ -61928,9 +61929,41 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       "url",
       resp.url
     );
-    const url = new URL(resp.url);
+    const client = new _TaskCommandRouterClientImpl(
+      serverClient,
+      taskId,
+      sandboxId,
+      isV2,
+      resp.url,
+      resp.jwt,
+      logger2,
+      profile
+    );
+    logger2.debug(
+      "Successfully initialized command router client",
+      "task_id",
+      taskId
+    );
+    return client;
+  }
+  constructor(serverClient, taskId, sandboxId, isV2, serverUrl, jwt, logger2, profile) {
+    this.serverClient = serverClient;
+    this.taskId = taskId;
+    this.sandboxId = sandboxId;
+    this.isV2 = isV2;
+    this.serverUrl = serverUrl;
+    this.jwt = jwt;
+    this.jwtExp = parseJwtExpiration(jwt, logger2);
+    this.logger = logger2;
+    this.profile = profile;
+    const transport = this.createTransport(serverUrl);
+    this.channel = transport.channel;
+    this.stub = transport.stub;
+  }
+  createTransport(routerUrl) {
+    const url = new URL(routerUrl);
     if (url.protocol !== "https:") {
-      throw new Error(`Task router URL must be https, got: ${resp.url}`);
+      throw new Error(`Task router URL must be https, got: ${routerUrl}`);
     }
     const host = url.hostname;
     const port = url.port ? parseInt(url.port) : 443;
@@ -61944,8 +61977,8 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       "grpc.keepalive_permit_without_calls": 1
     };
     let channel;
-    if (isLocalhost(profile)) {
-      logger2.warn(
+    if (isLocalhost(this.profile)) {
+      this.logger.warn(
         "Using insecure TLS (skip certificate verification) for task command router"
       );
       channel = (0, import_nice_grpc11.createChannel)(
@@ -61960,40 +61993,16 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
         channelConfig
       );
     }
-    const client = new _TaskCommandRouterClientImpl(
-      serverClient,
-      taskId,
-      sandboxId,
-      isV2,
-      resp.url,
-      resp.jwt,
-      channel,
-      logger2
-    );
-    logger2.debug(
-      "Successfully initialized command router client",
-      "task_id",
-      taskId
-    );
-    return client;
-  }
-  constructor(serverClient, taskId, sandboxId, isV2, serverUrl, jwt, channel, logger2) {
-    this.serverClient = serverClient;
-    this.taskId = taskId;
-    this.sandboxId = sandboxId;
-    this.isV2 = isV2;
-    this.serverUrl = serverUrl;
-    this.jwt = jwt;
-    this.jwtExp = parseJwtExpiration(jwt, logger2);
-    this.logger = logger2;
-    this.channel = channel;
     const self = this;
     const factory = (0, import_nice_grpc11.createClientFactory)().use(timeoutMiddleware).use(async function* authMiddleware(call, options) {
       options.metadata ??= new import_nice_grpc11.Metadata();
       options.metadata.set("authorization", `Bearer ${self.jwt}`);
       return yield* call.next(call.request, options);
     });
-    this.stub = factory.create(TaskCommandRouterDefinition, channel);
+    return {
+      channel,
+      stub: factory.create(TaskCommandRouterDefinition, channel)
+    };
   }
   close() {
     if (this.closed) {
@@ -62003,37 +62012,48 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     this.channel.close();
   }
   /** Run a unary RPC against the command router with the default retry policy. */
-  async callUnary(fn) {
-    return await callWithRetriesOnTransientErrors(
-      () => this.callWithAuthRetry(fn),
-      10,
-      // baseDelayMs
-      2,
-      // delayFactor
-      10,
-      // maxRetries
-      null,
-      // no overall deadline
-      () => this.closed
-    );
+  async callUnary(fn, options) {
+    const overallDeadlineMs = options?.timeoutMs !== void 0 ? Date.now() + options.timeoutMs : null;
+    try {
+      return await callWithRetriesOnTransientErrors(
+        () => this.callWithAuthRetry(() => {
+          const remainingMs = overallDeadlineMs !== null ? Math.max(1, overallDeadlineMs - Date.now()) : options?.timeoutMs;
+          return fn({
+            ...options,
+            timeoutMs: remainingMs
+          });
+        }),
+        10,
+        2,
+        10,
+        overallDeadlineMs,
+        () => this.closed,
+        [import_nice_grpc11.Status.DEADLINE_EXCEEDED, import_nice_grpc11.Status.CANCELLED]
+      );
+    } catch (err) {
+      if (overallDeadlineMs !== null && Date.now() >= overallDeadlineMs) {
+        throw new TimeoutError("Timeout expired");
+      }
+      throw err;
+    }
   }
   async execStart(request) {
     return await this.callUnary(() => this.stub.taskExecStart(request));
   }
-  async containerCreate(request) {
-    return await this.callUnary(() => this.stub.taskContainerCreate(request));
+  async containerCreate(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerCreate(request, callOptions), options);
   }
-  async containerGet(request) {
-    return await this.callUnary(() => this.stub.taskContainerGet(request));
+  async containerGet(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerGet(request, callOptions), options);
   }
   async containerList(request) {
     return await this.callUnary(() => this.stub.taskContainerList(request));
   }
-  async containerTerminate(request) {
-    await this.callUnary(() => this.stub.taskContainerTerminate(request));
+  async containerTerminate(request, options) {
+    await this.callUnary((callOptions) => this.stub.taskContainerTerminate(request, callOptions), options);
   }
-  async containerWait(request) {
-    return await this.callUnary(() => this.stub.taskContainerWait(request));
+  async containerWait(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerWait(request, callOptions), options);
   }
   async *execStdioRead(taskId, execId, fileDescriptor, deadline = null) {
     let srFd;
@@ -62229,15 +62249,15 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       throw err;
     }
   }
-  async refreshJwt() {
+  async refreshJwt(failedJwt) {
     let error;
     this.jwtRefreshLock = this.jwtRefreshLock.then(async () => {
       if (this.closed) {
         return;
       }
-      if (this.jwtExp !== null && this.jwtExp - Date.now() / 1e3 > 30) {
+      if (this.jwt !== failedJwt) {
         this.logger.debug(
-          "Skipping JWT refresh because expiration is far enough in the future",
+          "Skipping JWT refresh because another request already refreshed it",
           "task_id",
           this.taskId
         );
@@ -62250,11 +62270,24 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
           this.sandboxId,
           this.isV2
         );
-        if (resp.url !== this.serverUrl) {
-          this.logger.warn("Task router URL changed during session");
+        if (this.closed) {
+          return;
         }
+        const routerChanged = resp.url !== this.serverUrl;
+        const replacement = routerChanged ? this.createTransport(resp.url) : null;
+        const previousChannel = routerChanged ? this.channel : null;
         this.jwt = resp.jwt;
         this.jwtExp = parseJwtExpiration(resp.jwt, this.logger);
+        if (replacement) {
+          this.serverUrl = resp.url;
+          this.channel = replacement.channel;
+          this.stub = replacement.stub;
+          try {
+            previousChannel?.close();
+          } catch {
+          }
+          this.logger.warn("Task router URL changed during session; transport replaced");
+        }
       } catch (err) {
         error = err;
       }
@@ -62265,11 +62298,12 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     }
   }
   async callWithAuthRetry(func) {
+    const attemptedJwt = this.jwt;
     try {
       return await func();
     } catch (err) {
       if (err instanceof import_nice_grpc11.ClientError && err.code === import_nice_grpc11.Status.UNAUTHENTICATED) {
-        await this.refreshJwt();
+        await this.refreshJwt(attemptedJwt);
         return await func();
       }
       throw err;
@@ -62291,6 +62325,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     while (true) {
       try {
         const timeoutMs = deadline !== null ? Math.max(0, deadline - Date.now()) : void 0;
+        const attemptedJwt = this.jwt;
         const request = TaskExecStdioReadRequest.create({
           taskId,
           execId,
@@ -62312,7 +62347,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
           return;
         } catch (err) {
           if (err instanceof import_nice_grpc11.ClientError && err.code === import_nice_grpc11.Status.UNAUTHENTICATED && !didAuthRetry) {
-            await this.refreshJwt();
+            await this.refreshJwt(attemptedJwt);
             didAuthRetry = true;
             continue;
           }
@@ -63222,7 +63257,8 @@ var SidecarService = class {
           env: envDict,
           workdir: params?.workdir ?? "",
           secretIds
-        })
+        }),
+        { timeoutMs: params?.timeoutMs, signal: params?.signal }
       );
     } catch (err) {
       if (err instanceof import_nice_grpc14.ClientError) {
@@ -63252,7 +63288,8 @@ var SidecarService = class {
           taskId,
           containerName: name,
           includeTerminated: params?.includeTerminated ?? false
-        })
+        }),
+        { timeoutMs: params?.timeoutMs, signal: params?.signal }
       );
     } catch (err) {
       if (err instanceof import_nice_grpc14.ClientError && err.code === import_nice_grpc14.Status.NOT_FOUND) {
@@ -63308,18 +63345,21 @@ var SidecarContainer = class {
     return this.#filesystem;
   }
   /** Block until the sidecar container exits, and return its exit code. */
-  async wait() {
+  async wait(params) {
     if (this.#result && this.#result.status !== 0 /* GENERIC_STATUS_UNSPECIFIED */) {
       return getReturnCode(this.#result) ?? 0;
     }
     const [taskId, client] = await this.#access.commandRouter();
+    const deadlineAtMs = params?.timeoutMs !== void 0 ? Date.now() + params.timeoutMs : null;
     while (true) {
+      const remainingMs = deadlineAtMs !== null ? Math.max(1, deadlineAtMs - Date.now()) : params?.timeoutMs;
       const resp = await client.containerWait(
         TaskContainerWaitRequest.create({
           taskId,
           containerId: this.containerId,
           timeout: CONTAINER_WAIT_POLL_TIMEOUT_SECONDS
-        })
+        }),
+        { ...params, timeoutMs: remainingMs }
       );
       const result = resp.result;
       if (!result || result.status === 0 /* GENERIC_STATUS_UNSPECIFIED */) {
@@ -63359,14 +63399,17 @@ var SidecarContainer = class {
    */
   async terminate(params) {
     const [taskId, client] = await this.#access.commandRouter();
+    const deadlineAtMs = params?.timeoutMs !== void 0 ? Date.now() + params.timeoutMs : null;
+    const remainingMs = () => deadlineAtMs !== null ? Math.max(1, deadlineAtMs - Date.now()) : params?.timeoutMs;
     await client.containerTerminate(
       TaskContainerTerminateRequest.create({
         taskId,
         containerId: this.containerId
-      })
+      }),
+      { timeoutMs: remainingMs(), signal: params?.signal }
     );
     if (params?.wait) {
-      return this.wait();
+      return this.wait({ ...params, timeoutMs: remainingMs() });
     }
   }
 };
@@ -64658,7 +64701,7 @@ var Sandbox2 = class _Sandbox {
     const [taskId, commandRouterClient] = await this.#getCommandRouter();
     const request = TaskSnapshotFilesystemRequest.create({
       taskId,
-      snapshotId: (0, import_uuid2.v4)(),
+      snapshotId: params?.snapshotId ?? (0, import_uuid2.v4)(),
       ttlSeconds: wireTtlSeconds
     });
     const response = await commandRouterClient.snapshotFilesystem(request, {
@@ -64778,7 +64821,7 @@ var Sandbox2 = class _Sandbox {
     const request = buildTaskSnapshotDirectoryRequestProto(
       taskId,
       path2,
-      (0, import_uuid2.v4)(),
+      params?.snapshotId ?? (0, import_uuid2.v4)(),
       wireTtlSeconds,
       params
     );
diff --git a/dist/index.d.cts b/dist/index.d.cts
index 9b2f092b923c931edd32b39aeb7af55ec1c9ec1b..7811a92d1427fc5dae3668ccf000ad71edd58063 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -6663,6 +6663,8 @@ type ImageFromNameParams = {
     /** Modal Environment to resolve the named Image in. */
     environment?: string;
 };
+/** Optional deadline and cancellation parameters for {@link ImageService#fromId client.images.fromId()}. */
+type ImageFromIdParams = TimeoutOptions;
 /**
  * A closure that hydrates the registry-auth {@link Secret} (if any) and builds
  * the {@link ImageRegistryConfig} for the base build layer. It runs at build
@@ -6687,7 +6689,7 @@ declare class ImageService {
      *
      * @param imageId - Image ID.
      */
-    fromId(imageId: string): Promise<Image>;
+    fromId(imageId: string, params?: ImageFromIdParams): Promise<Image>;
     /**
      * Reference a named {@link Image} that was previously published.
      *
@@ -7247,11 +7249,11 @@ declare class TaskCommandRouterClientImpl {
     /** Run a unary RPC against the command router with the default retry policy. */
     private callUnary;
     execStart(request: TaskExecStartRequest): Promise<TaskExecStartResponse>;
-    containerCreate(request: TaskContainerCreateRequest): Promise<TaskContainerCreateResponse>;
-    containerGet(request: TaskContainerGetRequest): Promise<TaskContainerGetResponse>;
+    containerCreate(request: TaskContainerCreateRequest, options?: TimeoutOptions): Promise<TaskContainerCreateResponse>;
+    containerGet(request: TaskContainerGetRequest, options?: TimeoutOptions): Promise<TaskContainerGetResponse>;
     containerList(request: TaskContainerListRequest): Promise<TaskContainerListResponse>;
-    containerTerminate(request: TaskContainerTerminateRequest): Promise<void>;
-    containerWait(request: TaskContainerWaitRequest): Promise<TaskContainerWaitResponse>;
+    containerTerminate(request: TaskContainerTerminateRequest, options?: TimeoutOptions): Promise<void>;
+    containerWait(request: TaskContainerWaitRequest, options?: TimeoutOptions): Promise<TaskContainerWaitResponse>;
     execStdioRead(taskId: string, execId: string, fileDescriptor: FileDescriptor, deadline?: number | null): AsyncGenerator<TaskExecStdioReadResponse>;
     execStdinWrite(taskId: string, execId: string, offset: number, data: Uint8Array, eof: boolean): Promise<TaskExecStdinWriteResponse>;
     execPoll(taskId: string, execId: string, deadline?: number | null): Promise<TaskExecPollResponse>;
@@ -7501,12 +7503,12 @@ type SidecarCreateParams = {
     secrets?: Secret[];
     /** Working directory of the sidecar container. */
     workdir?: string;
-};
+} & TimeoutOptions;
 /** Options for {@link SidecarService#get SidecarService.get()}. */
 type SidecarGetParams = {
     /** If true, return the latest container with the name even if it has terminated. */
     includeTerminated?: boolean;
-};
+} & TimeoutOptions;
 /** Options for {@link SidecarService#list SidecarService.list()}. */
 type SidecarListParams = {
     /** If true, include terminated containers. */
@@ -7518,7 +7520,7 @@ type SidecarExecParams = SandboxExecParams;
 type SidecarTerminateParams = {
     /** If true, wait for the sidecar container to terminate. */
     wait?: boolean;
-};
+} & TimeoutOptions;
 /**
  * Creates and manages sidecar containers inside a Sandbox.
  *
@@ -7561,7 +7563,7 @@ declare class SidecarContainer {
     /** Namespace for filesystem APIs scoped to this sidecar container. */
     get filesystem(): SandboxFilesystem;
     /** Block until the sidecar container exits, and return its exit code. */
-    wait(): Promise<number>;
+    wait(params?: TimeoutOptions): Promise<number>;
     /**
      * Check if the sidecar container has finished running.
      *
@@ -7569,7 +7571,7 @@ declare class SidecarContainer {
      */
     poll(): Promise<number | null>;
     terminate(): Promise<void>;
-    terminate(params: {
+    terminate(params: SidecarTerminateParams & {
         wait: true;
     }): Promise<number>;
 }
@@ -7848,6 +7850,12 @@ type SandboxTerminateParams = {
 };
 /** Optional parameters for {@link Sandbox#snapshotFilesystem Sandbox.snapshotFilesystem()}. */
 type SandboxSnapshotFilesystemParams = {
+    /**
+     * Optional caller-owned idempotency key for this snapshot request. Reusing
+     * the same value retries the same provider operation and returns its
+     * original Image instead of creating a duplicate.
+     */
+    snapshotId?: string;
     /**
      * Overall budget for the snapshot call, in milliseconds. Defaults to
      * 55000. If it elapses before a snapshot completes, the call is cancelled
@@ -7873,6 +7881,12 @@ type SandboxReloadVolumesParams = {
 };
 /** Optional parameters for {@link Sandbox#snapshotDirectory Sandbox.snapshotDirectory()}. */
 type SandboxSnapshotDirectoryParams = {
+    /**
+     * Optional caller-owned idempotency key for this snapshot request. Reusing
+     * the same value retries the same provider operation and returns its
+     * original Image instead of creating a duplicate.
+     */
+    snapshotId?: string;
     /**
      * Overall budget for the snapshot call, in milliseconds. Defaults to
      * 55000. If it elapses before a snapshot completes, the call is cancelled
@@ -8177,6 +8191,8 @@ declare class ModalClient {
 type TimeoutOptions = {
     /** Timeout for this call, interpreted as a duration in milliseconds */
     timeoutMs?: number;
+    /** Signal that cancels the in-flight provider call. */
+    signal?: AbortSignal;
 };
 type RetryOptions = {
     /** Number of retries to take. */
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 9b2f092b923c931edd32b39aeb7af55ec1c9ec1b..7811a92d1427fc5dae3668ccf000ad71edd58063 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -6663,6 +6663,8 @@ type ImageFromNameParams = {
     /** Modal Environment to resolve the named Image in. */
     environment?: string;
 };
+/** Optional deadline and cancellation parameters for {@link ImageService#fromId client.images.fromId()}. */
+type ImageFromIdParams = TimeoutOptions;
 /**
  * A closure that hydrates the registry-auth {@link Secret} (if any) and builds
  * the {@link ImageRegistryConfig} for the base build layer. It runs at build
@@ -6687,7 +6689,7 @@ declare class ImageService {
      *
      * @param imageId - Image ID.
      */
-    fromId(imageId: string): Promise<Image>;
+    fromId(imageId: string, params?: ImageFromIdParams): Promise<Image>;
     /**
      * Reference a named {@link Image} that was previously published.
      *
@@ -7247,11 +7249,11 @@ declare class TaskCommandRouterClientImpl {
     /** Run a unary RPC against the command router with the default retry policy. */
     private callUnary;
     execStart(request: TaskExecStartRequest): Promise<TaskExecStartResponse>;
-    containerCreate(request: TaskContainerCreateRequest): Promise<TaskContainerCreateResponse>;
-    containerGet(request: TaskContainerGetRequest): Promise<TaskContainerGetResponse>;
+    containerCreate(request: TaskContainerCreateRequest, options?: TimeoutOptions): Promise<TaskContainerCreateResponse>;
+    containerGet(request: TaskContainerGetRequest, options?: TimeoutOptions): Promise<TaskContainerGetResponse>;
     containerList(request: TaskContainerListRequest): Promise<TaskContainerListResponse>;
-    containerTerminate(request: TaskContainerTerminateRequest): Promise<void>;
-    containerWait(request: TaskContainerWaitRequest): Promise<TaskContainerWaitResponse>;
+    containerTerminate(request: TaskContainerTerminateRequest, options?: TimeoutOptions): Promise<void>;
+    containerWait(request: TaskContainerWaitRequest, options?: TimeoutOptions): Promise<TaskContainerWaitResponse>;
     execStdioRead(taskId: string, execId: string, fileDescriptor: FileDescriptor, deadline?: number | null): AsyncGenerator<TaskExecStdioReadResponse>;
     execStdinWrite(taskId: string, execId: string, offset: number, data: Uint8Array, eof: boolean): Promise<TaskExecStdinWriteResponse>;
     execPoll(taskId: string, execId: string, deadline?: number | null): Promise<TaskExecPollResponse>;
@@ -7501,12 +7503,12 @@ type SidecarCreateParams = {
     secrets?: Secret[];
     /** Working directory of the sidecar container. */
     workdir?: string;
-};
+} & TimeoutOptions;
 /** Options for {@link SidecarService#get SidecarService.get()}. */
 type SidecarGetParams = {
     /** If true, return the latest container with the name even if it has terminated. */
     includeTerminated?: boolean;
-};
+} & TimeoutOptions;
 /** Options for {@link SidecarService#list SidecarService.list()}. */
 type SidecarListParams = {
     /** If true, include terminated containers. */
@@ -7518,7 +7520,7 @@ type SidecarExecParams = SandboxExecParams;
 type SidecarTerminateParams = {
     /** If true, wait for the sidecar container to terminate. */
     wait?: boolean;
-};
+} & TimeoutOptions;
 /**
  * Creates and manages sidecar containers inside a Sandbox.
  *
@@ -7561,7 +7563,7 @@ declare class SidecarContainer {
     /** Namespace for filesystem APIs scoped to this sidecar container. */
     get filesystem(): SandboxFilesystem;
     /** Block until the sidecar container exits, and return its exit code. */
-    wait(): Promise<number>;
+    wait(params?: TimeoutOptions): Promise<number>;
     /**
      * Check if the sidecar container has finished running.
      *
@@ -7569,7 +7571,7 @@ declare class SidecarContainer {
      */
     poll(): Promise<number | null>;
     terminate(): Promise<void>;
-    terminate(params: {
+    terminate(params: SidecarTerminateParams & {
         wait: true;
     }): Promise<number>;
 }
@@ -7848,6 +7850,12 @@ type SandboxTerminateParams = {
 };
 /** Optional parameters for {@link Sandbox#snapshotFilesystem Sandbox.snapshotFilesystem()}. */
 type SandboxSnapshotFilesystemParams = {
+    /**
+     * Optional caller-owned idempotency key for this snapshot request. Reusing
+     * the same value retries the same provider operation and returns its
+     * original Image instead of creating a duplicate.
+     */
+    snapshotId?: string;
     /**
      * Overall budget for the snapshot call, in milliseconds. Defaults to
      * 55000. If it elapses before a snapshot completes, the call is cancelled
@@ -7873,6 +7881,12 @@ type SandboxReloadVolumesParams = {
 };
 /** Optional parameters for {@link Sandbox#snapshotDirectory Sandbox.snapshotDirectory()}. */
 type SandboxSnapshotDirectoryParams = {
+    /**
+     * Optional caller-owned idempotency key for this snapshot request. Reusing
+     * the same value retries the same provider operation and returns its
+     * original Image instead of creating a duplicate.
+     */
+    snapshotId?: string;
     /**
      * Overall budget for the snapshot call, in milliseconds. Defaults to
      * 55000. If it elapses before a snapshot completes, the call is cancelled
@@ -8177,6 +8191,8 @@ declare class ModalClient {
 type TimeoutOptions = {
     /** Timeout for this call, interpreted as a duration in milliseconds */
     timeoutMs?: number;
+    /** Signal that cancels the in-flight provider call. */
+    signal?: AbortSignal;
 };
 type RetryOptions = {
     /** Number of retries to take. */
diff --git a/dist/index.js b/dist/index.js
index 83068162e5c4b650f1046bcaf3ba8b4f9f73b7b9..9c21288c03e3d630d4ec29810e1f04d4522d370e 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -56902,9 +56902,9 @@ var ImageService = class {
    *
    * @param imageId - Image ID.
    */
-  async fromId(imageId) {
+  async fromId(imageId, params) {
     try {
-      const resp = await this.#client.cpClient.imageFromId({ imageId });
+      const resp = await this.#client.cpClient.imageFromId({ imageId }, params);
       return new Image2(this.#client, resp.imageId, "");
     } catch (err) {
       if (err instanceof ClientError7 && err.code === Status7.NOT_FOUND)
@@ -61828,6 +61828,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
   jwtExp;
   jwtRefreshLock = Promise.resolve();
   logger;
+  profile;
   closed = false;
   static async tryInit(serverClient, taskId, sandboxId, isV2, logger2, profile) {
     let resp;
@@ -61856,9 +61857,41 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       "url",
       resp.url
     );
-    const url = new URL(resp.url);
+    const client = new _TaskCommandRouterClientImpl(
+      serverClient,
+      taskId,
+      sandboxId,
+      isV2,
+      resp.url,
+      resp.jwt,
+      logger2,
+      profile
+    );
+    logger2.debug(
+      "Successfully initialized command router client",
+      "task_id",
+      taskId
+    );
+    return client;
+  }
+  constructor(serverClient, taskId, sandboxId, isV2, serverUrl, jwt, logger2, profile) {
+    this.serverClient = serverClient;
+    this.taskId = taskId;
+    this.sandboxId = sandboxId;
+    this.isV2 = isV2;
+    this.serverUrl = serverUrl;
+    this.jwt = jwt;
+    this.jwtExp = parseJwtExpiration(jwt, logger2);
+    this.logger = logger2;
+    this.profile = profile;
+    const transport = this.createTransport(serverUrl);
+    this.channel = transport.channel;
+    this.stub = transport.stub;
+  }
+  createTransport(routerUrl) {
+    const url = new URL(routerUrl);
     if (url.protocol !== "https:") {
-      throw new Error(`Task router URL must be https, got: ${resp.url}`);
+      throw new Error(`Task router URL must be https, got: ${routerUrl}`);
     }
     const host = url.hostname;
     const port = url.port ? parseInt(url.port) : 443;
@@ -61872,8 +61905,8 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       "grpc.keepalive_permit_without_calls": 1
     };
     let channel;
-    if (isLocalhost(profile)) {
-      logger2.warn(
+    if (isLocalhost(this.profile)) {
+      this.logger.warn(
         "Using insecure TLS (skip certificate verification) for task command router"
       );
       channel = createChannel2(
@@ -61888,40 +61921,16 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
         channelConfig
       );
     }
-    const client = new _TaskCommandRouterClientImpl(
-      serverClient,
-      taskId,
-      sandboxId,
-      isV2,
-      resp.url,
-      resp.jwt,
-      channel,
-      logger2
-    );
-    logger2.debug(
-      "Successfully initialized command router client",
-      "task_id",
-      taskId
-    );
-    return client;
-  }
-  constructor(serverClient, taskId, sandboxId, isV2, serverUrl, jwt, channel, logger2) {
-    this.serverClient = serverClient;
-    this.taskId = taskId;
-    this.sandboxId = sandboxId;
-    this.isV2 = isV2;
-    this.serverUrl = serverUrl;
-    this.jwt = jwt;
-    this.jwtExp = parseJwtExpiration(jwt, logger2);
-    this.logger = logger2;
-    this.channel = channel;
     const self = this;
     const factory = createClientFactory2().use(timeoutMiddleware).use(async function* authMiddleware(call, options) {
       options.metadata ??= new Metadata2();
       options.metadata.set("authorization", `Bearer ${self.jwt}`);
       return yield* call.next(call.request, options);
     });
-    this.stub = factory.create(TaskCommandRouterDefinition, channel);
+    return {
+      channel,
+      stub: factory.create(TaskCommandRouterDefinition, channel)
+    };
   }
   close() {
     if (this.closed) {
@@ -61931,37 +61940,48 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     this.channel.close();
   }
   /** Run a unary RPC against the command router with the default retry policy. */
-  async callUnary(fn) {
-    return await callWithRetriesOnTransientErrors(
-      () => this.callWithAuthRetry(fn),
-      10,
-      // baseDelayMs
-      2,
-      // delayFactor
-      10,
-      // maxRetries
-      null,
-      // no overall deadline
-      () => this.closed
-    );
+  async callUnary(fn, options) {
+    const overallDeadlineMs = options?.timeoutMs !== void 0 ? Date.now() + options.timeoutMs : null;
+    try {
+      return await callWithRetriesOnTransientErrors(
+        () => this.callWithAuthRetry(() => {
+          const remainingMs = overallDeadlineMs !== null ? Math.max(1, overallDeadlineMs - Date.now()) : options?.timeoutMs;
+          return fn({
+            ...options,
+            timeoutMs: remainingMs
+          });
+        }),
+        10,
+        2,
+        10,
+        overallDeadlineMs,
+        () => this.closed,
+        [Status10.DEADLINE_EXCEEDED, Status10.CANCELLED]
+      );
+    } catch (err) {
+      if (overallDeadlineMs !== null && Date.now() >= overallDeadlineMs) {
+        throw new TimeoutError("Timeout expired");
+      }
+      throw err;
+    }
   }
   async execStart(request) {
     return await this.callUnary(() => this.stub.taskExecStart(request));
   }
-  async containerCreate(request) {
-    return await this.callUnary(() => this.stub.taskContainerCreate(request));
+  async containerCreate(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerCreate(request, callOptions), options);
   }
-  async containerGet(request) {
-    return await this.callUnary(() => this.stub.taskContainerGet(request));
+  async containerGet(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerGet(request, callOptions), options);
   }
   async containerList(request) {
     return await this.callUnary(() => this.stub.taskContainerList(request));
   }
-  async containerTerminate(request) {
-    await this.callUnary(() => this.stub.taskContainerTerminate(request));
+  async containerTerminate(request, options) {
+    await this.callUnary((callOptions) => this.stub.taskContainerTerminate(request, callOptions), options);
   }
-  async containerWait(request) {
-    return await this.callUnary(() => this.stub.taskContainerWait(request));
+  async containerWait(request, options) {
+    return await this.callUnary((callOptions) => this.stub.taskContainerWait(request, callOptions), options);
   }
   async *execStdioRead(taskId, execId, fileDescriptor, deadline = null) {
     let srFd;
@@ -62157,15 +62177,15 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
       throw err;
     }
   }
-  async refreshJwt() {
+  async refreshJwt(failedJwt) {
     let error;
     this.jwtRefreshLock = this.jwtRefreshLock.then(async () => {
       if (this.closed) {
         return;
       }
-      if (this.jwtExp !== null && this.jwtExp - Date.now() / 1e3 > 30) {
+      if (this.jwt !== failedJwt) {
         this.logger.debug(
-          "Skipping JWT refresh because expiration is far enough in the future",
+          "Skipping JWT refresh because another request already refreshed it",
           "task_id",
           this.taskId
         );
@@ -62178,11 +62198,24 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
           this.sandboxId,
           this.isV2
         );
-        if (resp.url !== this.serverUrl) {
-          this.logger.warn("Task router URL changed during session");
+        if (this.closed) {
+          return;
         }
+        const routerChanged = resp.url !== this.serverUrl;
+        const replacement = routerChanged ? this.createTransport(resp.url) : null;
+        const previousChannel = routerChanged ? this.channel : null;
         this.jwt = resp.jwt;
         this.jwtExp = parseJwtExpiration(resp.jwt, this.logger);
+        if (replacement) {
+          this.serverUrl = resp.url;
+          this.channel = replacement.channel;
+          this.stub = replacement.stub;
+          try {
+            previousChannel?.close();
+          } catch {
+          }
+          this.logger.warn("Task router URL changed during session; transport replaced");
+        }
       } catch (err) {
         error = err;
       }
@@ -62193,11 +62226,12 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     }
   }
   async callWithAuthRetry(func) {
+    const attemptedJwt = this.jwt;
     try {
       return await func();
     } catch (err) {
       if (err instanceof ClientError10 && err.code === Status10.UNAUTHENTICATED) {
-        await this.refreshJwt();
+        await this.refreshJwt(attemptedJwt);
         return await func();
       }
       throw err;
@@ -62219,6 +62253,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
     while (true) {
       try {
         const timeoutMs = deadline !== null ? Math.max(0, deadline - Date.now()) : void 0;
+        const attemptedJwt = this.jwt;
         const request = TaskExecStdioReadRequest.create({
           taskId,
           execId,
@@ -62240,7 +62275,7 @@ var TaskCommandRouterClientImpl = class _TaskCommandRouterClientImpl {
           return;
         } catch (err) {
           if (err instanceof ClientError10 && err.code === Status10.UNAUTHENTICATED && !didAuthRetry) {
-            await this.refreshJwt();
+            await this.refreshJwt(attemptedJwt);
             didAuthRetry = true;
             continue;
           }
@@ -63150,7 +63185,8 @@ var SidecarService = class {
           env: envDict,
           workdir: params?.workdir ?? "",
           secretIds
-        })
+        }),
+        { timeoutMs: params?.timeoutMs, signal: params?.signal }
       );
     } catch (err) {
       if (err instanceof ClientError13) {
@@ -63180,7 +63216,8 @@ var SidecarService = class {
           taskId,
           containerName: name,
           includeTerminated: params?.includeTerminated ?? false
-        })
+        }),
+        { timeoutMs: params?.timeoutMs, signal: params?.signal }
       );
     } catch (err) {
       if (err instanceof ClientError13 && err.code === Status13.NOT_FOUND) {
@@ -63236,18 +63273,21 @@ var SidecarContainer = class {
     return this.#filesystem;
   }
   /** Block until the sidecar container exits, and return its exit code. */
-  async wait() {
+  async wait(params) {
     if (this.#result && this.#result.status !== 0 /* GENERIC_STATUS_UNSPECIFIED */) {
       return getReturnCode(this.#result) ?? 0;
     }
     const [taskId, client] = await this.#access.commandRouter();
+    const deadlineAtMs = params?.timeoutMs !== void 0 ? Date.now() + params.timeoutMs : null;
     while (true) {
+      const remainingMs = deadlineAtMs !== null ? Math.max(1, deadlineAtMs - Date.now()) : params?.timeoutMs;
       const resp = await client.containerWait(
         TaskContainerWaitRequest.create({
           taskId,
           containerId: this.containerId,
           timeout: CONTAINER_WAIT_POLL_TIMEOUT_SECONDS
-        })
+        }),
+        { ...params, timeoutMs: remainingMs }
       );
       const result = resp.result;
       if (!result || result.status === 0 /* GENERIC_STATUS_UNSPECIFIED */) {
@@ -63287,14 +63327,17 @@ var SidecarContainer = class {
    */
   async terminate(params) {
     const [taskId, client] = await this.#access.commandRouter();
+    const deadlineAtMs = params?.timeoutMs !== void 0 ? Date.now() + params.timeoutMs : null;
+    const remainingMs = () => deadlineAtMs !== null ? Math.max(1, deadlineAtMs - Date.now()) : params?.timeoutMs;
     await client.containerTerminate(
       TaskContainerTerminateRequest.create({
         taskId,
         containerId: this.containerId
-      })
+      }),
+      { timeoutMs: remainingMs(), signal: params?.signal }
     );
     if (params?.wait) {
-      return this.wait();
+      return this.wait({ ...params, timeoutMs: remainingMs() });
     }
   }
 };
@@ -64586,7 +64629,7 @@ var Sandbox2 = class _Sandbox {
     const [taskId, commandRouterClient] = await this.#getCommandRouter();
     const request = TaskSnapshotFilesystemRequest.create({
       taskId,
-      snapshotId: uuidv42(),
+      snapshotId: params?.snapshotId ?? uuidv42(),
       ttlSeconds: wireTtlSeconds
     });
     const response = await commandRouterClient.snapshotFilesystem(request, {
@@ -64706,7 +64749,7 @@ var Sandbox2 = class _Sandbox {
     const request = buildTaskSnapshotDirectoryRequestProto(
       taskId,
       path2,
-      uuidv42(),
+      params?.snapshotId ?? uuidv42(),
       wireTtlSeconds,
       params
     );
