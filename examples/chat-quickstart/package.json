{
  "name": "@opengeni/example-chat-quickstart",
  "version": "0.0.15",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env.local server.ts",
    "server": "bun --env-file=.env.local server.ts",
    "onboard": "bun --env-file=.env.local onboard.ts",
    "build": "bun build server.ts onboard.ts --target=bun --packages=external --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opengeni/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
