// Labels for a planned operations panel; not cancellation or retry logic.
export const panelLabels = {
  cancel: 'AbortSignal cancellation requested',
  retry: 'Automatic retry pending',
  audit: 'Audit enabled',
};
